# Project context

This is a digital implementation of a simplified WoW TCG ruleset.
Always check the rules in `References/wow_rules.txt` before implementing any
keyword or card effect — do not rely on general CCG knowledge (Magic,
Hearthstone) since WoW TCG has its own specific rulings.

Card data lives in: `data/cards.csv` (plus `data/tokens.csv` for effect-created
tokens — both load into one `CardDatabase` via `load_all()`)
Active playtest scene: `renderer/tests/playtest.gd` (human vs AI, full UI)
Engine lives in: `game_logic/` — see Architecture section below.

---

## Implementing a card — required steps

1. Find the card in `data/cards.csv` (grep by name). Read the card image to get exact card text. If `image_path` is blank but the image file exists under `assets/cards/`, fill in `image_path` now.
2. **Confirm the card text with the user before writing any code.** Also check `References/wow_rules.txt` for any keywords or mechanics.
3. Implement: engine actions in `game_logic/stack_resolver.gd`, AI heuristics in `game_logic/ai/base_ai.gd` if needed.
4. Write headless test scenarios in `game_logic/tests/test_scenarios.gd` covering the new mechanic. User runs these to verify correctness before finalizing.
5. Update `data/cards.csv`: set `data_status=verified`, `engine_status=implemented`, fill `power_text` and `effects` recipe. Do this last, after tests pass.
6. **Ask the user whether the newly implemented card should be added to any deck(s)** in `decks/recommended_ai/*.json`. Do not add it unprompted.

---

## Architecture (game_logic/)

### Core invariants — violating these is a bug:

- **Rules functions never touch the bus.** They return `Array[GameEvent]`. The caller emits.
- **Rules functions never read Godot nodes.** No `get_tree()`, no autoloads, no scene refs. Pass `db` when def lookups are needed.
- **Zone moves go through `GameLogic.move_card()` only.** Never mutate `zone.card_ids` or `card.zone_id` directly.
- **Derived stats are never cached on CardInstance.** Call `state.get_atk(id, db)` etc. Caching causes stale-read bugs.
- **Fold inner events:** `events.append_array(inner_call(...))`.

### Key files:

| File | Role |
|---|---|
| `game_logic/state/game_state.gd` | Single source of truth. All mutable game data. |
| `game_logic/state/player_state.gd` | Per-player flags (resources, pet_capacity, hero_instance_id…) |
| `game_logic/state/card_instance.gd` | Runtime card state (zone, damage, keywords, buffs…) |
| `game_logic/stack_resolver.gd` | ALL rules validation (`can_submit`) and resolution (`_resolve_*`). Priority/chain logic. |
| `game_logic/turn_manager.gd` | Phase advancement, turn start (ready all, clear summoning sickness). |
| `game_logic/actions/primitives.gd` (class `GameLogic`) | Low-level helpers: `move_card`, `deal_damage`, `check_destroyed`, `exhaust_card`, `heal`. `deal_damage` only places damage; callers run `check_destroyed` as a separate step (needed for simultaneous combat damage). |
| `game_logic/events/game_event.gd` | Event constructors. Add typed constructors for every new event. |
| `game_logic/ai/base_ai.gd` | AI action selection — `get_reasonable_actions` → scored → best chosen. |
| `renderer/board_renderer.gd` | Visual layer: card nodes, highlights, animations. No rules logic. |
| `renderer/tests/playtest.gd` | The running playtest scene. Wires everything together. AI + human. |
| `input/input_router.gd` | Human input → PendingAction. The only place that calls `submit_action` for humans. |

### Priority / chain flow (rule 410):

```
submit_action(state, action, db) → pushes to pending_actions, proposer keeps priority
pass_priority(state, db)         → consecutive_passes++; at 2: resolve top OR close window
```

**When both players pass with chain empty:**
- If `combat_attack_window` → `_close_attack_window` (protect point → defend window)
- If `combat_defend_window` → `_do_combat_conclusion` (damage)
- If `pending_enter_play_effect` non-empty → stall (scene must handle)
- Otherwise → emit `priority_window_closed` → `TurnManager` advances phase

### Mandatory immediate-resolution choices (bypass pending_actions):

Some choices must resolve without going through the stack:
- `StackResolver.choose_discard(state, card_id, db)` — pending discard
- `StackResolver.choose_pet_sacrifice(state, card_id, db)` — pet uniqueness violation
- `StackResolver.choose_control_discard(state, card_id, db)` / `decline_control_discard(state, db)` — Infernal's start-of-turn "discard a card, or target opponent gains control". Unlike a mandatory discard the player MAY decline: control changes (rule 401.3 — card moves to the new controller's ally_row, `just_summoned` set, pet uniqueness re-checked for the new controller). `pass_priority` is also hard-blocked while pending; in the UI the pass button becomes the decline option ("Give up control [Ctrl+Space]") and hand cards highlight red for the discard. Declining is gated behind Ctrl+Space (or the pass button / a card click) — plain Space is absorbed with a hint, so an accidental tap can't hand the card to the opponent.

These are called directly (not via `submit_action`). The blocking guard in `can_submit` prevents anything else while they're pending.

### Combat step flow (rule 602):

```
propose_combat resolves →
  attacker exhausts + combat_started event
  combat_attack_window = true → attack_window_opened event
    [both players get priority — can use instants, ally powers, etc.]
  both pass → _close_attack_window →
    protect point (if protectors exist) OR
    combat_defend_window = true → defend_window_opened event
      [both players get priority again]
    both pass → _do_combat_conclusion → damage → combat_concluded
```

**Ally activated powers** (e.g. Grimdron) are legal during attack/defend windows even for the non-turn player — `_can_use_ally_power` only requires `priority_player == source_player` (no `turn_player` restriction), because "use only on your turn" is an explicit card-text restriction, not the default.

---

## Card data format (data/cards.csv columns)

```
set_id, card_num, name, cost, card_type, faction, class_restriction, card_subtype,
subtype_detail, printed_atk, dmg_type, printed_health, rarity, keywords,
power_text, data_status, engine_status, effects, image_path
```

**`effects` string** — pipe-separated segments, each colon-separated:

| Segment | Meaning |
|---|---|
| `activated_power:COST:EFFECT:AMOUNT:DMG_TYPE:TARGETS:EXTRACOST` | Ally/equipment activated power. `EFFECT` includes `draw`. `EXTRACOST` (optional 7th field) is a card-specific extra cost paid alongside resources+activate, e.g. `exhaust_hero` (Mooncloth Robe). `no_activate` marks a plain payment power (701.2) whose printed cost has no [Activate] tap symbol — no self-exhaust, no summoning sickness, repeatable each turn (Hierophant Caydiem: "3 →..."). `put_damage_self:N` is the same idea but the extra cost is self-damage (Acolyte Demia). `activate_put_damage_self:N` is self-damage that KEEPS the [Activate] tap symbol — the source also exhausts and has summoning sickness (Kena Shadowbrand). `rfg_allies:N` removes N ally cards from your graveyard from the game as a cost (Augustus Corpsemonger — the specific N are auto-chosen in graveyard order, see `data/rules_deviations.md`). `sacrifice_ally` destroys a chosen ally in your party as a cost; pair with `TARGETS=friendly_ally` so the target picker only offers your own allies, the sacrifice may be the source itself (Bizzik Sparkcog). `sacrifice_self` destroys the SOURCE itself as a cost and implies no [Activate] tap symbol — no exhaust, no summoning sickness (Kavai the Wanderer: "1, Destroy Kavai →..."; Mana Agate `azeroth_57`, an ongoing Ability in the hero_row — the source may be an ally OR any in-play card); the destroy is paid at resolution, so if an opponent kills the source in response it no-ops and the announced effect still resolves (matching the printed rules, where costs are paid at announcement). `EFFECT` value `destroy_ally` destroys the (any-party) target ally — pair with `TARGETS=ally` (Augustus). `EFFECT` value `destroy_ability_or_equipment` destroys a target in-play ability OR equipment card, either player's — pair with `TARGETS=ability_or_equipment` (candidates = Burn Away's + Shattering Blow's pools via `get_destroy_kind_candidates`; even the highlight probe requires one to exist) |
| `equipment:SLOT:DEF` | Marks an Equipment card: `SLOT` (chest/back/neck/…) drives slot uniqueness (rule 414.3); `DEF` is defense (0 = no damage prevention). Equipment enters the hero row |
| `ongoing` | Marks an Ability as ongoing (rule 305.2a — stays in play instead of resolve-and-graveyard). Non-totem ongoing abilities enter the controller's hero_row (`_resolve_play_ongoing_ability`). `is_ongoing_def()` also routes an **Instant** Ability with this flag to `play_ability` (enters play) instead of `play_instant` in both `_action_type_for`s |
| `totem[:ELEMENT]` | Marks a Totem (rule 305.3 — Searing Totem): an ability **ally** that enters the controller's **ally_row** (not hero_row), can't be proposed as an attacker (`is_totem_def` gate in `get_legal_attackers` / `_can_propose_combat`), but can be attacked/targeted like any ally. ATK/health come from the CSV `atk`/`health` cols. Pairs with an `ongoing` segment. Play is instant-speed (type line "Instant Ability") |
| `heal_party_each_turn:AMOUNT` | Ongoing Totem trigger (Healing Stream Totem): at the start of **each** turn (both players'), heals AMOUNT damage from the controller's hero and each ally in their party (the totem included). No choice — fires inline in `TurnManager._apply_start_of_turn_effects` (each_turn branch) |
| `opposing_allies_cant_ready` | Static aura (Earthbind Totem): opposing **allies** (ally_row cards, totems included; heroes/equipment/resources unaffected) can't ready during their controllers' ready step. Live scan in `TurnManager._ready_blocked` — lifts the moment the aura card leaves play. The UI shows a ⛓ badge on every ready-blocked card (`TurnManager.is_ready_blocked` probe → `CardNode.set_ready_locked`, refreshed from playtest `_refresh_ready_lock_badges`; Entangling Roots hosts get it too) |
| `ongoing_damage_each_turn:AMOUNT:DMG_TYPE` | Ongoing Totem power: at the start of **each** turn, the totem's controller deals AMOUNT DMG_TYPE damage to a chosen target hero or ally. Collected in `TurnManager._collect_ongoing_turn_triggers` (ready step, turn player's totems first — 501.1a); each opens a mandatory **target choice** resolved via `StackResolver.choose_totem_target()` (direct call, `pending_totem_target_player` hard-blocks `can_submit`/`pass_priority` — like the strike/reveal choices). Once the target is picked, the trigger goes on the **chain** as a `resolve_totem_trigger` link and a normal priority window opens (turn player first, rule 501.1a/410) — either player may respond with an instant before the damage lands; `_resolve_totem_trigger` re-checks the target (709.2a) and deals it through the prevention pipeline, then `totem_next` opens the next queued trigger. Scene handles `totem_target_required`: AI auto-targets (opposing kill else opposing hero), human targets via `start_totem_targeting`; the emitted `action_proposed` drives the window drain. Windows are sequential (one per trigger) and the non-turn AI passes them — see `data/rules_deviations.md` "Searing Totem" |
| `on_enter:EFFECT:AMOUNT:DMG_TYPE` | Enter-play triggered effect (rule 501.1). Fires as the ally resolves into play (`_resolve_play_ally` sets `pending_enter_play_effect` + emits `enter_play_target_required`). **Targeted** enter-play effects (Taz'dingo `deal_damage_to_target`, Ghank `destroy_exhausted_damaged_ally`) are a two-step chain flow: the controller picks a target via a mandatory point (`choose_enter_play_target`, gated by `pending_enter_play_effect`), and that submission puts the trigger **on the chain** with the target chosen. `submit_action` captures the effect onto the chain link's `params._effect_dict` and **clears `pending_enter_play_effect` at announcement** so the ensuing priority window is REAL — the opponent can respond before it resolves (e.g. sacrifice/bounce the targeted ally to its own power). `_resolve_choose_enter_play_target` reads the effect from the link and re-checks the target (706/709.2a, fizzles if it left play). Clearing the marker at announcement (not resolution) is the fix for the dead-window bug where `can_submit`'s guard blocked every response |
| `on_enter:create_token:TOKEN_DEF_ID[:N]` | Enter-play token creation (Mya, Dragonling Wrangler `dark_portal_184`): "When Mya enters play, put a Mechanical Dragonling ally token with 1 ATK and 1 health into play." Non-targeted and mandatory, so it fires **inline** in `_bring_ally_into_play` — no choice, nothing on the chain. `TOKEN_DEF_ID` is a def id from `data/tokens.csv`. `StackResolver._put_token_into_play` mints the instance (`GameLogic.create_token`, id `<controller>_token_<n>` off `GameState.token_counter`, `owner` = the creating player) and brings it in through `_bring_ally_into_play` like any ally — summoning sickness, its own on_enter triggers, Watcher Mal'wi reactions, and the Pet/Unique uniqueness checks all apply. Emits `token_created`. The token is a normal ally in every respect except that it ceases to exist when it leaves play — see the Tokens section |
| `create_token:TOKEN_DEF_ID[:N]` (quest reward) | Same effect as a quest reward segment (Tooga's Quest `azeroth_359`): "Reward: Put a unique Turtle ally token named Tooga with 1 ATK and 1 health into play." Handled in `_apply_quest_reward`, same `_put_token_into_play` path |
| `rfg_self_next_turn:draw:N` | Delayed self-removal carried by a token (Tooga): "At the start of your next turn, remove Tooga from the game. If you do, draw N cards." Fires in `TurnManager._apply_start_of_turn_effects` (controller's turn only), gated on `CardInstance.created_on_turn` so it can't fire on the turn the token was created. The removal is a plain `move_card` to RFG — **not** a destroy, so no `card_destroyed` and no destruction triggers; the draw is the "if you do" rider and only happens because the removal did. Putting the trigger on the TOKEN rather than on the quest is what makes the fizzle free: a token destroyed before then simply isn't in play to be scanned, so no removal and no draw. Resolved inline, not on the chain — see `data/rules_deviations.md` "Tooga" |
| `destroy_target:ally` | Destroy a target ally (Vanquish-style) |
| `destroy_target:exhausted_ally` | Destroy a target **exhausted** ally, either party (Coup de Grâce `azeroth_93`, 2, Ability — Assassination). A subset of the ally-only destroy restriction (`_instant_targets_ally_only` includes `exhausted_ally`): the target must be an ally AND exhausted, checked at submission (`_destroy_requires_exhausted`) and re-checked at resolution (`_is_exhausted_ally`, 706 — fizzles if it left play OR readied in response). The highlight probe (`_targeted_play_has_legal_target`) also filters to exhausted allies, so the card goes dark when none is exhausted. AI (`_targeted_instant_actions`): destroys the single most valuable exhausted OPPOSING ally by `card_value_score` (always worth it — a spent attacker) |
| `except_friendly_attached` | Rider on a `destroy_target:ability` segment (Purge `azeroth_114`, 1, Instant Ability — Elemental, Shaman): "Destroy target ability **unless it's attached to a friendly hero or ally**." Narrows Burn Away's pool — an in-play ability is illegal when it is an attachment whose HOST is a hero or ally controlled by the CASTER. "Friendly" is judged on the host's controller, not the attachment's, so an opponent's Entangling Roots sitting on your own ally is protected from your own Purge (the card's deliberate blind spot); an attachment on a *weapon* (Windfury Weapon) isn't on a hero or ally and stays targetable. `_is_destroyable_ability` gates submission (`_can_play_instant`/`_can_play_ability`), the highlight probe (`_targeted_play_has_legal_target` — the card goes dark when every in-play ability is friendly-attached) and the resolution re-check (706/4217 — a host that changes control mid-chain fizzles the destroy). AI: inherits Burn Away's `_targeted_instant_actions` branch (opposing targets with cost ≥ spell cost; at cost 1 that's effectively always) — not tagged as a combat instant |
| `gouge_cant_ready` | Pairs with an `exhaust_target:hero_or_ally` segment (Gouge `azeroth_99`, 1, Instant Ability — Combat Combo): "It can't ready during its controller's next ready step." After the exhaust resolves (applied even if the target was already exhausted), sets a one-shot `gouge_skip_ready` counter on the target + emits `card_ready_locked`. `TurnManager._ready_blocked` returns true while the counter is present (drives the ⛓ badge probe); `_enter_ready` consumes it exactly at the target controller's next ready step — the card skips that one ready step whether or not something else also blocked it, then readies normally after. Survives the end-of-turn buff sweep (it's a counter, not a turns-buff), so it times correctly when cast on the opponent's turn. AI: Gouge is tagged `combat_instant_exhaust` (held; answers an opposing attacker — hero or ally — via `exhaust_attacker_action`, fizzling the proposal per 601.3); the ready-lock rider is not modeled |
| `target_cost_min:N` / `target_cost_max:N` | Printed-**cost band** rider on a `destroy_target:ally` segment (Trophy Kill `dark_portal_40`, 3, Instant Ability — Marksmanship, Hunter: "Destroy target ally with cost 4 or more" = `destroy_target:ally|target_cost_min:4`; Prey on the Weak `dark_portal_85`, 2, Instant Ability — Assassination, Rogue: "…cost 4 or less" = `destroy_target:ally|target_cost_max:4`). A **rider, not a target kind** — the target stays an ordinary `ally`, so every ally-only path (`_instant_targets_ally_only`, the friendly-only variant, the router's `can_submit`-filtered target list) applies unchanged and the two mirror cards need no UI code. Both bands are **inclusive**, so a cost-4 ally is a legal target for either card. The cost measured is the target's **printed** cost (`printed_cost` off its `CardDef`, `cost_base` for a hypothetical X-cost ally) — never `get_play_cost`: an in-play ally was paid for long ago and cost modifiers apply to cards being PLAYED. `_destroy_cost_band_ok` gates submission (both `_can_play_instant` and `_can_play_ability`), the highlight probe (`_targeted_play_has_legal_target` — the card goes dark when no ally in play falls in the band) and the resolution re-check (706/4217). AI: no new code — the generic `destroy_target:ally` branch of `_targeted_instant_actions` enumerates opposing allies and is filtered by `can_submit` (the band) then `_destroy_is_worth_it` (target cost ≥ spell cost, which the `min` band satisfies by construction) |
| `on_destroyed:EFFECT[:ARGS]` | Death-triggered effect fired when the card is destroyed (rule 703, `_fire_on_destroyed`, after the card reaches the graveyard). `deal_damage_aoe:AMOUNT:DMG_TYPE:opposing` auto-fires (no choice). `destroy_target:ally` (Boneshanks `dark_portal_201`: "When [this] is destroyed, destroy target ally.") is a **mandatory targeted** death trigger: it queues a `{card_id, controller}` entry in `pending_death_triggers` and opens a direct-call choice (`pending_death_target_player`, `death_target_required` event) resolved by the destroyed card's controller via `StackResolver.choose_death_target()` — NOT the chain; `can_submit`/`pass_priority` hard-block while pending. Drained one at a time; chained deaths (destroying another Boneshanks) queue behind. Fizzles only when no ally is in play (`get_death_target_targets`). Targets any ally either party (may be forced onto the controller's own board). Human controller is PROMPTED even in hotseat (the choice is board-public; scene points the router at the controller). AI: `BaseAI.choose_death_target` picks the highest-value enemy ally, else the least valuable own ally when forced. See `data/rules_deviations.md` "Boneshanks" |
| `return_to_hand:ally` | "Put target ally into its owner's hand" (Withdraw `azeroth_172`, 3, Instant Ability). Ally-only (`_instant_targets_ally_only`), instant speed. Resolution re-checks 706 + `_is_ally` (fizzles if the ally left play), then `GameLogic.move_card` to the owner's hand — leaving play resets damage/exhaust/buffs (400.6a, generalized in `move_card` to ANY play→non-play move, not just graveyard) and destroys attachments (400.5). Bounced in response to an opposing removal spell aimed at it, that spell fizzles at the 709.2a recheck; a bounced attacker/defender fizzles a pending combat proposal (601.3). Emits `card_returned_to_hand`. AI: tagged `combat_instant_save_bounce` (held, never blind-played); `BaseAI.save_bounce_action` plays it ONLY to interrupt opposing targeted removal on the chain aimed at our ally (destroy_target/destroy_ally, or lethal targeted damage incl. modal damage modes and ally-power damage; ally cost ≥ Withdraw's cost) — deliberately NEVER to dodge a combat attack (an attack costs the opponent no card; the bounce burns our card AND re-pays the ally's cost) |
| `form:N` | **Form (N) type-line tag** (rule 414.3b tag-count uniqueness — Bear Form `azeroth_18`, Bash `azeroth_17`, Cat Form `dark_portal_19`, Claw `dark_portal_20`; all Form (1)). Always paired with `ongoing` — Forms are ongoing Abilities living in the hero_row. A player may control at most N in-play Forms: a second one triggers an immediate sacrifice choice mirroring the Pet/Unique flow (`_check_form_uniqueness` → `pending_form_sacrifice_*` → `choose_form_sacrifice`, direct call; `can_submit` hard-blocks). Playing the new Form IS the shapeshift — the AI (and normally a human) destroys the OLD one (scene keeps `new_card_id` from the event) |
| `form_break:TAG` | Form self-destroy condition (glossary Bear/Cat Form): "Destroy this card when you strike with a weapon or play a non-TAG ability." Druid Feral Forms use `form_break:Feral` (Claw's "Feral Combo" tag matches by substring). Checked at ability RESOLUTION (`_check_form_break_ability` in `_resolve_play_instant` + `_resolve_play_ongoing_ability` — covers plain, ongoing, totem and attachment ability plays) and at the strike point (`_check_form_break_strike` in `choose_strike`). Weapon *activated powers* and armor prevention never break a form; ally/equipment/quest plays don't either. Travel Form would omit the segment; Shadowform's inverted "when you play a Holy ability" would need a future `form_break_on:TAG`. See `data/rules_deviations.md` "Form break timing" |
| `hero_atk_while_attacking:N` | Cat form ongoing grant: the controller's HERO has +N ATK **while attacking**. Live in `GameState._aura_atk_mods` (hero_row source scan), gated on `is_attacking` — defender-independent, so it's included in `assume_attacking` forecasts AND the `get_legal_attackers` hero gate (which now probes `get_atk(hero, db, true)`; a 0-ATK hero with a while-attacking bonus — Cat Form, a Ryn Dreamstrider buff — is a legal attacker, matching the printed rules). Never cached. Bear form reuses the existing `hero_has_protector` flag |
| `on_destroyed:pay_return_hand:COST` | Bear/Cat Form death trigger: "When [this] is destroyed, you may pay COST. If you do, put it into your hand." Opens a direct-call choice (`pending_form_return_*`, `form_return_opened`, `choose_form_return(state, pay, db)` — like the whelp bounce; `can_submit`/`pass_priority` hard-block) only when affordable; paying returns the card to hand IMMEDIATELY (deviation — see `data/rules_deviations.md` "Form return timing"). Bash and Claw do NOT have this clause. AI always pays (`BaseAI.choose_form_return`) |
| `remove_attackers:CONDITION` | "Remove all attackers from combat" gated on a condition (Blink `azeroth_48`: `hero_defending` — the caster's hero is the CURRENT defender, i.e. the defend window is open with `combat_defender` = their hero; during the attack window the hero is only a *proposed* defender, so the clause no-ops — the strictness is deliberate, "defending" exists only after the protect point, 602.3). Resolution clears `state.combat_attacker` and emits `attacker_removed_from_combat`; per 602.4 the combat step does NOT end immediately — the window plays out and the conclusion's 603.1b check then deals no damage. The attacker stays exhausted. 1-on-1 combat: "all attackers" = the single attacker. Future ally-defending cards get a new CONDITION token. AI: tagged `combat_instant_evasion` (held); `BaseAI.evasion_action` plays it in the DEFEND window only, when the AI's hero is the defender and attacker ATK > 3 or hero HP < 10 |
| `atk_swing:A1:A2` | "Target ally has +3 ATK this turn. Target ally has -3 ATK this turn." (Ravenous Bite `azeroth_44`, 2, Instant Ability — Beast Mastery, Hunter). Two **independent, mandatory** ally targets announced in order with the play (`target_id` gets A1, `target_id_2` gets A2). Not modal (the printed text has no "Choose one —") and not Chain Lightning's "up to N" list: both picks are required, **both respect Untargetable** (706 — no Multi-Shot-style exception), and the SAME ally may be named twice (the halves then cancel, which is legal and sometimes forced when only one ally is in play). Each half re-checks its own target at resolution, so killing/bouncing one target fizzles only that half. Buffs are `Buff` stat `atk`, duration `turns`:1 — the existing end-of-turn sweep gives "this turn" expiry for free, correct even when cast on the opponent's turn. `GameState.get_atk` now floors at 0 (a character can't have negative ATK) while KEEPING the raw negative buff, so a later +ATK counts from the true value. UI: two sequential picks via the `_targeting_first_target` two-phase flow (`InputRouter._is_atk_swing`), prompts "select the ally that gets +3/-3 ATK"; clicking the spell during phase 2 steps back to phase 1. AI: tagged `combat_instant_atk_swing` (held) — see `BaseAI.atk_swing_action` |
| `deal_damage_and_heal:DMG:DMG_TYPE:HEAL` | "Your hero deals DMG damage to target hero or ally **and** heals HEAL damage from **another** target hero or ally" (Shock and Soothe `dark_portal_100`, 4, Instant Ability — Elemental, Shaman). Long-standing hero-power / ally-power effect key (Grennan, Hierophant Caydiem) now reachable from a **hand card** — the play_instant / play_ability paths. Two mandatory targets announced with the play (`target_id` = damage, `heal_target_id` = heal), and they must be **DISTINCT** ("another"). Both are real targets — Untargetable blocks either slot (706) — and each half is re-checked independently at resolution, so answering one in response fizzles only that half. The heal is unconditional (not per-damage) and lands inline; the damage packet goes through the prevention pipeline tagged `from_ability` (Chromatic Cloak +1; a fire version would double under World in Flames). `is_damage_and_heal_def` gates submission (`_can_play_damage_and_heal`) and the highlight probe (`_has_two_legal_hero_or_ally_targets` — two distinct legal characters must exist). UI: two sequential picks via the `_targeting_first_target` two-phase flow (`InputRouter._is_instant_damage_and_heal`, the same flow the Grennan power uses); clicking the spell during phase 2 steps back to phase 1. AI (`_instant_damage_and_heal_actions`): damage an opposing character, heal one of OUR damaged ones; with no damaged friendly it holds the card unless the damage kills the target. NOT a `COMBAT_INSTANT_TAGS` card — the combat-instant path only supplies `target_id`, which can't satisfy the two-slot announce |
| `exhaust_target:ally` | Exhaust a target ally (Exhaustion, `azeroth_159`, Instant Ability). Variant `exhaust_target:hero_or_ally` (Bash) also accepts heroes — exhausting an attacking HERO whose proposal is on the chain fizzles it the same way (601.3). Ally-only (`_instant_targets_ally_only`), instant speed; resolution re-checks 706 + `_is_ally`, then `GameLogic.exhaust_card` (no-op if already exhausted). Aimed at a proposed attacker while its combat proposal is on the chain, the 601.3 recheck (`attacker.is_exhausted`) fizzles the proposal — same interrupt path as Litori's `target_cant_attack`; too late once the attack window opens. AI: tagged `combat_instant_exhaust` in `BaseAI.COMBAT_INSTANT_TAGS` (held, never blind-played); `BaseAI.exhaust_attacker_action` answers an opposing propose_combat aimed at our side with Litori's worth math (attacker must be an **ally**, answer must be affordable), wired into both `decide_action`s after `hero_disable_action` |
| `grant_keyword_target:KIND:KEYWORD` | "Target ally has KEYWORD this turn" — the general keyword-grant recipe (Sneak `azeroth_152`, 1, Instant Ability, Alliance: `grant_keyword_target:ally:elusive`; Into the Fray `azeroth_153`, 1, Ability, Horde: `grant_keyword_target:friendly_ally:ferocity`). `KIND` is an existing target kind: `ally` (either party) or `friendly_ally` ("in your party", the Fall Back restriction), so the whole submission path — `_instant_needs_target`, `_instant_targets_ally_only`, `_instant_targets_friendly_ally_only`, the highlight probe and the router's `can_submit`-filtered target list — covers it with no new UI code. The grant is a `Buff` stat `grant_<KEYWORD>`, duration `turns`:1, read by `_has_keyword` (the Hidden Enemies mechanism): the end-of-turn sweep gives "this turn" expiry for free (correct even when cast on the opponent's turn) and leaving play clears it. Resolution re-checks the target (`_grant_keyword_target_ok` — 706/4217 + still-an-ally + friendly where required), emits `keyword_granted`. **Sneak is a combat interrupt:** cast in response to a combat proposal aimed at our ally, the 601.3 legality recheck already tests `_has_keyword(defender, "elusive")`, so the proposal fizzles and the attacker never exhausts — Litori/Exhaustion's interrupt point, but targeting our own side; too late once the attack window opens (elusive restricts who may be CHOSEN as defender). AI, deliberately narrower than the printed targeting in both cards: it only ever sees its OWN allies (handing the opponent elusive or ferocity is never wanted, even though Sneak's "target ally" makes it legal for a human), and a `ferocity` grant is offered only on a **summoning-sick, ATK > 0** ally — on an ally that can already attack the card changes nothing. Sneak is tagged `combat_instant_save_elusive` (held, never blind-played) and plays from `BaseAI.elusive_save_action`, wired into all three `decide_action`s |
| `all_allies_keyword:KEYWORD` | **Board-wide keyword aura** — "Ongoing: All allies have KEYWORD" (Lust for Battle `azeroth_154`, 4, Ability, Horde: ferocity; From the Shadows `azeroth_151`, 5, Ability, Alliance: elusive). Always paired with `ongoing`, so the card lives in its controller's hero_row. **"All allies" is literal and symmetric**: BOTH parties' allies, the opponent's included — a Horde Lust for Battle gives the Alliance player ferocity too. Unlike `opposing_allies_cant_attack` / `opposing_cant_protect` the scan is therefore not relative to a controller (`_ally_keyword_aura` sweeps every player's hero_row + ally_row). Because it can't live on the cards it affects, the grant is read **live inside `StackResolver._has_keyword`**, which gained an optional trailing `state` param for it: that is what makes it cover allies that enter play AFTER the aura resolved and makes the grant lift the instant the aura leaves play, with no bookkeeping. Every gate site that governs a keyword passes `state`; the `_is_ally` guard inside keeps the grant off heroes (an ally_row card, so totems DO get it). Elusive is "can't be **proposed as** a defender" (glossary), so From the Shadows funnels all attacks at the heroes but does NOT stop an elusive Protector from protecting (602.2). AI: no proactive modeling (both are played as vanilla develop-step abilities), but GenericAI's lethal detectors ask `_enters_with_ferocity` rather than the printed keyword, so an ally in hand is correctly seen as able to swing the turn it's played while a ferocity aura is out — without that the AI would miss real lethals |
| `exhaust_all_opposing` | "Exhaust all opposing heroes and allies" (War Stomp `dark_portal_137`, 3, Instant Ability, Horde). Non-targeted mass exhaust — hits Untargetable characters (706 restricts link targets only), needs no announce, spares the caster's side. In response to an opposing combat proposal on the chain, exhausting the attacker fizzles it via the 601.3 recheck — Exhaustion's interrupt, board-wide. AI: tagged `combat_instant_exhaust`; the `mass` gate in `exhaust_attacker_action` submits it with NO `target_id` and lets it answer an attacking HERO (which ally-only Exhaustion can't) |
| `requires_hero_race:RACE` | Deckbuilding restriction only (rule 100.2b — "[Race] Hero Required", War Stomp: Tauren). No in-game effect; enforced in `DeckManager.authorize_deck_def` (`required_hero_race`) by substring-matching RACE against the hero's tags column ("Tauren Shaman"; covers multi-word races like Night Elf) |
| `sarmoth_taunt` | Opposing characters that can attack this must attack only this |
| `heal_x_from_target:DMG_TYPE` | Hero power: pay X resources → heal X from target hero or ally (Boris) |
| `reveal_pick:CARD_TYPE:N` | Quest reward: reveal the top N cards; put a revealed card of `CARD_TYPE` (`Equipment`/`Ally`/`Ability`; matches the parsed `CardDef.card_type`, so `Instant Ally`→`Ally`) into hand, rest to the bottom of the deck in revealed order. If at least one matches, sets `pending_reveal_pick_*` and emits `reveal_pick_opened`; the scene resolves it via `StackResolver.choose_reveal_pick()` (direct call, not the stack — like pet sacrifice; `can_submit`/`pass_priority` hard-block while pending). The choice ALWAYS opens (even with no match) so the player sees the revealed cards; the browser shows every revealed card graveyard-style, non-matching ones tinted red and non-selectable, a matching card MUST be picked before OK, and if none match the player clicks OK (empty pick — `choose_reveal_pick(state, "")`) to send all revealed cards to the bottom. Big Game Hunter (Equipment:4), Kibler's Exotic Pets (Ally:3), Zapped Giants (Ability:3). `CARD_TYPE` `Any` makes every revealed card selectable. Optional trailing flags (order-independent, from field 4 on): `opponent` hands the PICK to the other player (The Princess Trapped), `to_top` puts the picked card back on TOP of the deck instead of into hand, `private` makes it a "look at" instead of a public reveal (both: It's a Secret to Everybody) — see `reveal_pick:Any:N:opponent` below |
| `reveal_pick:Any:N:opponent` | **Opponent-decided** reveal-pick (The Princess Trapped `azeroth_357`, Quest, pay 2): "Reveal the top two cards of your deck. Target opponent chooses one. Put that card into your hand and the other one on the bottom of your deck." First card whose DECIDER differs from its owner, so the pending choice now carries both: `GameState.pending_reveal_pick_player` is the OWNER (whose deck was revealed, whose hand the pick goes to) and `pending_reveal_pick_chooser` is the DECIDER. Every blocker (`can_submit` / `pass_priority`) still keys off the owner field — only input routing and AI pick-quality key off the chooser. `reveal_pick_opened` carries `chooser`; `choose_reveal_pick` is unchanged (the picked card still goes to the OWNER's hand, the rest to the bottom of the OWNER's deck). "Target opponent" is auto-chosen — see `data/rules_deviations.md` "The Princess Trapped". UI: the reveal browser opens for the chooser via `_route_choice(chooser, "public")` — in hotseat the revealed cards are shown to both players and only the input is re-pointed (no hand hiding, no handoff). AI: `_pick_ai_reveal(candidates, hostile)` inverts its ranking when choosing FOR the opponent — it hands them the worst revealed card |
| `reveal_pick:Any:N:to_top:private` | **Look-at-and-stack** reveal-pick (It's a Secret to Everybody `azeroth_354`, Quest, pay 1): "Look at the top three cards of your deck. Put one on the top of your deck and the rest on the bottom." Two flags on the shared reveal-pick machinery. `to_top` (`GameState.pending_reveal_pick_to_top`): the picked card is **not moved at all** — the revealed cards never left the top of the deck, so once the others are pushed to the bottom in revealed order it is back on top by itself; nothing reaches the hand. `private` (`pending_reveal_pick_private`): a "look at" is the chooser's alone, so the scene routes the browser through `_route_choice(chooser, "private")` instead of `"public"`, and the shared game log never names the kept card (`reveal_pick_resolved` carries `to_top`). In hotseat both players share a screen so privacy is nominal; it is what a future two-device build keys off to send the opponent nothing. With `Any` a pick is always available, so the empty-acknowledgement branch only comes up on an empty deck. AI: unchanged `_pick_ai_reveal` (keeps the highest-cost card) |
| `qmode:EFFECT[:ARGS]` + `qchoice_both_race:RACE` | **Quest reward choice** ("Choose one: …; or draw a card. If your hero is a RACE, you may choose both" — Hidden Enemies `dark_portal_302`, A New Plague `dark_portal_304`, Thwarting Kolkar Aggression `dark_portal_309`, Crown of the Earth `dark_portal_289`). Each `qmode:` segment wraps one reward mode; `qchoice_both_race:RACE` names the hero race (substring of the hero def's tags, like `requires_hero_race`) that unlocks picking BOTH modes, in an order of the completer's choosing. The quest resolves into a mandatory direct-call choice (`_open_quest_choice` → `pending_quest_choice_*` → `StackResolver.choose_quest_modes(state, [mode] or [first, second], db)`; `can_submit`/`pass_priority` hard-block). Unavailable modes can't be picked and knock out "both" (see `data/rules_deviations.md` "Quest reward choices"); chosen modes queue in `quest_mode_queue` and run front-first, each re-checked at run time. Mode effects: `draw:N`; `ally_ferocity_this_turn` (completer picks any in-play ally via `choose_quest_ferocity_target` — 706 respected; grant is a `grant_ferocity` Buff, turns:1, read by `_has_keyword`, so it expires end of turn); `each_player_destroys_ally` (gated on "an ally in your party"; every player WITH an ally picks their own sacrifice via `choose_plague_destroy`, completer first); `opponent_quest_face_down` ("target player" auto-opponent — deviation; the TARGET player picks which face-up quest flips via `choose_quest_facedown`; same spent face-down state as completion, no reward); `hand_to_deck_draw` (whole hand to the bottom of the deck in hand order, draw that many). UI: two-step centered popup (pick one/Both, then which resolves first), sub-picks via targeting mode (ferocity) or red mandatory modes (plague/facedown). AI: `choose_quest_modes` ALWAYS takes both when allowed (special mode first, draw last); single pick prefers the special mode only when useful (`_quest_mode_useful`); the plague AI holds the quest until the opponent has an ally; as Kolkar's target it flips its LEAST valuable quest (rarity, then cost, random tie) |
| `exhaust_allies:N` | Quest completion **cost** paid in exhausted allies (The Love Potion `azeroth_356`, Quest, cost 1): "Exhaust two allies in your party and pay (1) to complete this quest. Reward: Draw a card." The allies are announced WITH the completion (`ally_ids` param, like the graveyard-target rewards) and validated in `_can_use_quest` — N distinct READY cards in the completer's own ally_row (totems included; `get_quest_exhaust_candidates`). They are exhausted in `submit_action` on chain entry alongside the resource cost (rule 412.2, so the same ally can't pay for two announcements), and `retract_last` readies them again — that branch also refunds the quest's RESOURCE cost, which no quest did before. The `_skip_target_check` probe requires N ready allies to exist, so the quest goes dark otherwise. UI: clicking the quest opens a **pre-submission** picker (`InputRouter.start_ally_exhaust_selection` → `ally_exhaust_select_requested` → playtest `_on_ally_exhaust_select_requested`), a centered popup shaped like the armor-prevention one; the board is modal except the candidate allies (`_set_board_block` allowlist), clicking one toggles GREEN (selectable) ⇄ BLUE (selected), Confirm appears only at exactly N picks, and Cancel/Esc leaves the game state byte-identical because nothing was submitted. AI: `BaseAI._choose_quest_exhaust_allies` (overridable hook) spends the allies it would miss least — non-attackers first, then lowest `card_value_score` |
| `turn_start_discard_or_give_control` | At the start of your turn: discard a card, or the opponent gains control of this (Infernal). Fires in `TurnManager._apply_start_of_turn_effects`; resolves via `choose_control_discard` / `decline_control_discard` |
| `end_of_turn_damage_opposing:AMOUNT:DMG_TYPE` | At the end of your turn, this deals AMOUNT damage to each opposing hero and ally (Infernal). Fires in `TurnManager._apply_end_of_turn_effects` |
| `target_cant_attack` | Hero flip power (Litori Frostburn): target hero or ally can't attack this turn (`cannot_attack` restriction Buff, duration "turns":1, cleared by the end-of-turn buff sweep). Instant speed. Key timing (601.3 vs 602.1/602.4): played in RESPONSE to a combat proposal still on the chain, the proposal's legality recheck interrupts it — combat never starts and the attacker never exhausts. Played during an attack/defend window it's too late — "can't attack" is not a remove-from-combat effect; the current combat proceeds. AI: never blind-played; `BaseAI.hero_disable_action` answers an opposing propose_combat on the chain (hero defender taking lethal/≥4, or an ally cost ≥2 dying in a bad trade), holding it if a combat-instant kill is available |
| `deal_damage_to_target:AMOUNT:DMG_TYPE[:RESTRICTION]` | "Your hero deals AMOUNT damage to target hero or ally" (Quick Strike / Fire Blast). Optional 4th field: a `+`-joined restriction rider applied to a **surviving** target — `cannot_attack` (Frostbolt), `cannot_attack+cannot_protect` (Frost Shock). `_apply_damage_riders` places a restriction Buff (duration "turns":1, swept end of turn), reusing Litori's `cannot_attack` machinery; `cannot_protect` is enforced in `get_legal_protectors`. AI ignores the rider (still tagged `combat_instant_dmg`) — see `data/rules_deviations.md` |
| `deal_damage_to_target:X:DMG_TYPE` | **X-cost variant** (Aimed Shot `azeroth_32`, cost "1+X"): AMOUNT literal `X` = the announced `x_value` on the action. X-cost hand cards (`CardDef.cost_x`/`cost_base`, parsed from the CSV cost column) must announce `x_value >= 1` with the play (`_can_afford_play`); total cost `cost_base + X` is paid at submission (`_pay_cost`), refunded on retraction, and `GameState.get_play_cost(id, db, x)` takes the X. Highlight probes check affordability at X=1. UI: clicking the card opens the Boris-style X dialog (`x_select_requested`), then the normal targeting flow with X riding on `_instant_params`. AI (`_targeted_instant_actions`): X = target ally's HP (kill for the least) or max affordable vs the hero |
| `turn_start_heal_hero_and_pets:N` | Ongoing ability trigger (Spirit Bond `dark_portal_39`): "At the start of your turn, if you have a Pet, your hero heals N damage from itself and each of your Pets." Fires in `TurnManager._apply_start_of_turn_effects` (controller's turn only); the Pet condition (`card_subtype == "Pet"` in the ally_row) is checked at fire time — no Pet, no heal |
| `drain_heal_per_damage:N` | Pairs with a `deal_damage_to_target` segment (Steal Essence `azeroth_134`): after the instant's damage resolves, the casting hero heals N for each damage actually DEALT — armor prevention and 405.3 excess-beyond-fatal both reduce the heal (read from the `damage_dealt` events just produced). No-op on an undamaged hero. All existing `deal_damage_to_target` wiring (targeting, `_incoming_hero_damage`, AI damage math) applies unchanged. AI: tagged `combat_instant_dmg` (played for the damage; the drain heal is not modeled) |
| `discard_per_damage:N` | Pairs with a `deal_damage_to_target` segment (Mind Spike `azeroth_82`, Mind Blast `azeroth_80`) OR an `activated_power:...:deal_damage_to_target` power (Dark Cleric Ismantal `dark_portal_204`): after the damage resolves, the DAMAGED character's controller discards N cards for each damage actually DEALT (armor prevention and 405.3 excess-beyond-fatal reduce it, same as `drain_heal_per_damage`). Reuses the Mias pending-discard machinery (`_apply_discard_per_damage` sets `pending_discard_player`/`_count` on the target's controller + emits `discard_choice_opened`; scene resolves via `choose_discard`, AI auto-picks). No-op if that player's hand is empty. Enforcement in the `deal_damage_to_target` branches of `_resolve_play_instant` and `_resolve_use_ally_power`. AI plays these for the damage only — discard not modeled (see `data/rules_deviations.md`) |
| `mode:EFFECT[:ARGS]` | **Modal link** (rule 707.1c — "Choose one:", Natural Selection `azeroth_27`): each `mode:` segment wraps ONE standard effect segment (e.g. `mode:deal_damage_to_target:3:nature\|mode:heal_target:3`). The chosen mode index is announced at play time on the action (`params.mode`, validated in `_can_play_instant`/`_can_play_ability` via `is_modal_def`/`selected_mode`) — resolution dispatches ONLY the chosen mode's inner segment, so the engine receives "deal 3" OR "heal 3", never the choice. New inner effect `heal_target:N` = "your hero heals N damage from target hero or ally" (706 re-check, no-op on undamaged). v1 limit: every mode must target a hero or ally. UI: clicking the card opens inline "Choose one" buttons (`modal_choice_opened` → playtest `_on_modal_choice_opened`; Esc / clicking elsewhere cancels), then the normal targeting flow with the mode riding on the submission (`_targeting_mode`/`_instant_params`). AI: `BaseAI.get_modal_actions` enumerates EVERY mode × target so the full option space is visible to current/future AIs; `_modal_mode_playable` filters actual play to damage modes only for now, and Natural Selection itself is tagged `combat_instant_dmg` (held, ambushed with the damage mode announced — `_modal_dmg_mode_index`) |
| `multi_shot:AMOUNT:DMG_TYPE` | "Your hero deals AMOUNT damage to each of up to three target heroes and/or allies" (Multi-Shot, `azeroth_41`, Instant Ability). Up to 3 distinct hero-or-ally targets announced in order at play time (`target_id`/`target_id_2`/`target_id_3`; `_2` optional, `_3` requires `_2`), each takes the same AMOUNT — the hero is the damage source. Resolves each wave in printed order with its own destroy/game-over check (711.1). Validated by `_can_play_multi_shot` (in the `play_instant` path). Unlike Chain Lightning, **ALL three slots select rather than target** — every slot MAY pick an Untargetable character (706 exception). Reuses the Chain Lightning multi-target UI machinery (`_is_multi_target` in `input_router.gd`, action-type-aware). AI: `BaseAI._multi_shot_action` picks up to 3 enemy targets (killable allies first, then high-HP soak, then hero; opponents only) |
| `atk_vs_exhausted_defender:N` | Continuous self-modifier (Bala Silentblade `azeroth_226`): "+N ATK while attacking an exhausted hero or ally." Evaluated live in `GameState.get_atk` section (2) — on only while this card IS `combat_attacker` and the current `combat_defender` is exhausted, re-read at every call (a defender exhausted or readied mid-combat flips the bonus immediately; pairs with `on_attack_exhaust_target` to exhaust your own defender before the attack window). Never cached. Limits: the attack-cursor preview (`get_atk_if_attacking`) can't show it (no defender chosen yet), and the AI does not model the bonus when forecasting attacks (it still deals the boosted damage at conclusion) |
| `on_attack_exhaust_target` | Triggered attack power (Chops `dark_portal_32`, Voss Treebender `azeroth_266`): "When [this] attacks, you may exhaust target hero or ally." Fires as the combat step starts (602.1), AFTER the strike/ready-on-attack points and BEFORE the attack window — exhausting a ready Protector denies the protect point (602.2, the trigger's whole purpose); exhausting the proposed defender does NOT cancel the combat (601.3 already passed). Direct-call choice (`pending_attack_exhaust_*`, `StackResolver.choose_attack_exhaust(state, target_id, db)`, `""` = decline; `can_submit`/`pass_priority` hard-block while pending) — not on the chain, see `data/rules_deviations.md` "Attack-exhaust triggers". Targets = `get_attack_exhaust_targets` (any hero/ally, 706 untargetable respected). Human: targeting mode via `start_attack_exhaust_targeting`, Esc = decline (`_on_targeting_cancelled`). AI: `BaseAI.choose_attack_exhaust` exhausts the most dangerous ready legal protector (prefers one whose retaliation kills the attacker, else highest ATK), declines otherwise |
| `attach:TARGETS[:exhaust_it]` | Marks an ongoing Ability as an **attachment** (rule 400 — "Attach to target TARGETS"). Always paired with an `ongoing` segment. Targeted at play time like any targeted ability (`_instant_needs_target`/`_instant_targets_ally_only` cover `attach`; standard Esc-cancellable targeting flow, 706 Untargetable respected). Resolution (`_resolve_attach` in `_resolve_play_ongoing_ability`): re-checks the target (706/400.2 — if it left play or became Untargetable, the attachment goes to its owner's graveyard, `action_fizzled` reason `attach_target_gone`), then the card enters play in the shared `"attached"` zone with `CardInstance.attached_to` = host and its id in the host's `attachments` (both set BEFORE `move_card` per its contract), emits `card_attached`. Optional `exhaust_it` rider (Entangling Roots: "…and exhaust it") exhausts the host as part of the resolution. **Host leaves play → attachment destroyed by the game** (400.5/410.6c) — handled generically in `GameLogic.move_card` (any move of a host to a non-play zone destroys its attachments; moves within play, e.g. control change, keep them attached). Renderer: attachment tucks behind its host (`_attachment_hosts`, `ATTACH_PEEK` offset toward the opponent side, host z-bumped) and follows it on relayout; self-healed in `reconcile_from_state`. AI: `BaseAI._attach_actions` — buff attachments (`attached_buff`) go on the AI's own highest-ATK ally, debuff attachments on the opposing highest-ATK ally with cost ≥ spell cost (one action generated, the best target) |
| `attach_draw:N` | On-attach draw rider (Arcane Intellect: "Attach to target hero, and its controller draws a card"). Resolved inside `_resolve_attach` after the card attaches — the HOST's controller draws N (follows the attached hero, not the caster). `attach:hero` (vs `attach:ally`/`attach:hero_or_ally`) restricts attach targets to heroes only (`_attach_targets_hero_only`, enforced at submission in both `_can_play_instant`/`_can_play_ability` and at the `_resolve_attach` re-check). AI: `_attach_actions` puts a hero-only attachment on its OWN hero |
| `attached_max_hand:N` | Ongoing attachment grant (Arcane Intellect): "Attached hero's controller's maximum hand size is increased by three." Read live via `GameState.get_max_hand_size(player_id, db)` (base `PlayerState.max_hand_size` + N per attachment on the player's hero) — never cached; used by the 503.2a wrap-up discard (`TurnManager._next_turn`) and every AI hand-room gate |
| `attach_deal_damage:AMOUNT:DMG_TYPE` | On-attach damage rider (Fireball): "Attach to target hero or ally, and your hero deals AMOUNT DMG_TYPE damage to it." Resolved inside `_resolve_attach` AFTER the card attaches — the controller's HERO is the source, packets go through the prevention pipeline, and killing an ally host destroys the attachment with it (400.5). `attach:hero_or_ally` (vs `attach:ally`) makes heroes legal attach targets. AI: `_attach_actions` aims a damage attachment ONLY at the opposing hero (policy — any printed target stays legal for humans) |
| `attached_damage_turn_start:AMOUNT:DMG_TYPE` | Ongoing attachment trigger (Fireball): "At the start of your turn, your hero deals AMOUNT DMG_TYPE damage to attached character." Fires in `TurnManager._apply_start_of_turn_effects` (controller's turn only; the attachment's controller is the caster, wherever the host lives) — no choice, target is the host, packet pipeline |
| `hero_damage_unpreventable` | Static aura making damage **dealt by your hero** unpreventable (Lionheart Helm `azeroth_297`, Armor—Plate, Head (1), DEF 2). Read live off the SOURCE's controller's hero_row in `GameLogic.is_damage_unpreventable` — the source must BE that player's hero, so ally/totem/equipment-power damage is unaffected, and it shields nothing incoming (the helm's own DEF 2 still blocks normally). Covers every source by construction: `GameLogic.prevent` derives it from the source whenever a caller doesn't pass an explicit flag. The prevention POINT is also suppressed (`_prevention_offer`) — an offer that can't help is never offered, so the defender's armor stays ready. No AI modeling |
| `combat_damage_unpreventable` | The weapon-scoped form (Annihilator `azeroth_312`, Weapon—Axe, Melee (1), 3 ATK / strike 2, paired with `equipment:melee_weapon:0\|strike_cost:2`): only **combat** damage dealt **with that weapon** can't be prevented. `is_damage_unpreventable(..., is_combat = true)` additionally requires the weapon id in `state.combat_struck_weapons[hero]` (303.2b), so a hero striking a different weapon while the Annihilator sits in play gets ordinary preventable damage, and a hero ABILITY packet is preventable too. `_do_combat_conclusion` captures both sides' flags BEFORE clearing the associations (303.2a) and passes them explicitly. No AI modeling |
| `prevent_combat_damage_from_attacking_allies` | Self-referential prevention shield (Brother Rhone `dark_portal_157`, 0/1 Alliance Dwarf Paladin, Protector): "Prevent all combat damage that would be dealt to Brother Rhone by attacking allies." Unlike armor this is automatic and free — no decision point, no cost, and it protects an ALLY rather than a hero. Applied in `GameLogic.prevent` off the TARGET's own def, gated on the packet's `combat_attack` opt (set only for the attacker→defender packet of `_do_combat_conclusion`), and on the source being an ally_row card: an attacking HERO's combat damage lands, a defender's retaliation is unaffected, and non-combat ally damage (abilities, activated powers) lands. Unpreventable damage bypasses the shield without consuming it. AI, three parts, all driven by the pure probe `GameLogic.blocks_all_combat_damage` (so a future shield card inherits every one of them): (1) **defence** — `BaseAI.blocks_for_free` makes a free block a protector candidate in EVERY branch of `GenericAI.choose_protector` (and short-circuits `BaseAI.choose_protector`), ranked ahead of spending fodder but behind a `safe_lethal` block, which removes a card rather than stopping one attack. (2) **bait resistance** — protecting EXHAUSTS the protector (602.2), so the free block is once per turn and a cheap attacker can bait it away. `BaseAI.free_block_worth_spending` holds it back only when the opponent has another ready **ally** attacker (a hero attacker couldn't be blocked for free either) with strictly greater forecast ATK, AND the current attack is recoverable — kills nothing of ours and leaves the hero above `FREE_BLOCK_HERO_FLOOR`. A dead ally is permanent; an unblocked hit is not. (3) **offence** — `get_reasonable_actions` never proposes an ALLY attack against a defender that prevents all of its damage: no damage, no kill, and the defender doesn't even exhaust, so there is nothing to weigh. Source-dependent, so the HERO attacking Rhone (0/1, shield is ally-only) stays offered, as does an ally attacking *around* him — forcing the protect decision is exactly how the block gets stripped |
| `hero_ability_damage_bonus:N` | Static replacement aura (Chromatic Cloak `azeroth_282`, Armor—Cloth, Back, DEF 0): "If your hero would deal damage with an ability, it deals that amount of damage plus 1 instead." Applied in `defer_packets` (`_ability_bonus_amount`) off a `from_ability` packet tag set at ability-resolution packet sites (instants/abilities incl. modal & X-cost, ongoing-ability on-play damage, Chain Lightning / Multi-Shot / Flamestrike waves, attachments incl. Fireball's attach hit and turn-start burn). Hero flip POWERS, ally/equipment activated powers, totems, enter-play ally effects, and combat damage are not abilities — never tagged. +N per copy; applies BEFORE World in Flames' fire doubling (fixed order — see `data/rules_deviations.md` "Replacement-effect order"). No AI modeling — played as vanilla equipment |
| `hero_fire_damage_doubled` | Static replacement aura (World in Flames): "If your hero would deal fire damage, it deals double that amount of damage instead." Applied centrally in `defer_packets` (`_fire_doubled_amount`) off the per-packet `dmg_type` field, so prevention offers and per-damage riders see the doubled amount; hero-source packet sites (instants, Flamestrike AoE, hero powers, ongoing on-play, Fireball) pass their printed type along. Packets without a `dmg_type` — and combat damage, which has its own path (no fire-typed combat source exists) — never double. Per copy: two in play → ×4 |
| `attached_buff:ATK:HP` | Ongoing attachment stat grant (Mark of the Wild): "Attached ally has +ATK ATK and +HP health." Read live from the host's `attachments` list in `GameState.get_atk` (1b) and `get_max_hp` (`_attachment_stat_mods`) — never cached; ends the moment the attachment leaves play |
| `attached_cannot_ready` | Ongoing attachment restriction (Entangling Roots): "Attached ally can't ready during its controller's ready step." Live check in `TurnManager._enter_ready` (`_ready_blocked`) — the host skips the ready step (summoning sickness still clears); explicit ready effects (e.g. `ready_self_at_turn_end`) are NOT blocked |
| `opposing_allies_cant_attack` | Static aura (Lady Jaina Proudmoore): while a card with this flag is in play, that player's OPPONENT can't propose any of its **allies** as attackers (hero unaffected). Evaluated live via `_allies_attack_locked` in `get_legal_attackers` + `_can_propose_combat` — never cached. AI does not model the lock |
| `opposing_cant_protect` | Static aura (Hannah the Unstoppable): while a card with this flag is in play, that player's OPPONENT has NO legal protectors — neither Protector allies nor hero grants. Evaluated live via `_protect_locked` at the top of `get_legal_protectors` (returns `[]`) — never cached; the protect point simply never opens. AI does not model the lock |
| `hero_has_protector` | "Your hero has protector" (Draconian Deflector): while a card with this flag is in a player's hero_row, that player's ready hero is a legal protector (`get_legal_protectors`; 602.2b still applies — it can't protect itself). A hero that protects becomes the defender, so the 602.3 defend strike point fires for it — protecting heroes can retaliate with a weapon strike. `GameState.combat_protector` tracks who protected this combat (cleared at conclusion). AI: GenericAI protects with the hero only when no ally protector steps in, the hero stays above `HERO_ALL_OUT_HP` after the hit, and either the retaliation strike kills the attacker or a dying ally is worth the face damage (cost ≥ incoming ATK); BaseAI's highest-HP pick excludes the hero unless it's the only protector. `choose_strike_weapon` ALWAYS strikes while the hero is protecting (the opponent is attacking around the hero — likely the weapon's only use this turn); the Long-Range exception still holds (the strike would deal nothing) |
| `whelp_bounce` | Triggered equipment power (Green Whelp Armor): when an **attacking ally** deals combat damage that actually LANDS on the wielder's hero, the wielder MAY pay 2 to return that ally to its owner's hand. First *triggered* (not `[Activate]`/passive) equipment power. Detected at the end of `_do_combat_conclusion` (`_maybe_open_whelp_bounce`): attacker was an ally, defender was the wielder's hero, ≥1 damage landed (armor DEF/block absorbing all → no trigger), both still in play, wielder can afford 2. Opens a direct-call choice point (`pending_whelp_bounce_*`, `whelp_bounce_opened`) resolved via `StackResolver.choose_whelp_bounce(state, pay, db)` — like the strike point; `can_submit`/`pass_priority` hard-block while pending. Fires once per combat (single-attacker combat model). AI: `BaseAI.choose_whelp_bounce` bounces allies with cost ≥ 2. Scene: `_handle_whelp_bounce` (AI auto-resolves; any human — including the off-screen hotseat player, the choice is board-public — gets inline pay/decline buttons like the ready-on-attack point) |
| `graveyard_to_rfg:Ally:0:99:both` + `rfg_heal_per_card:N` | **Hand-ability graveyard exile** (Cannibalize `dark_portal_136`, 2, Ability — Horde, Undead Hero Required): "Remove any number of ally cards in graveyards from the game. Your hero heals N damage from itself for each card removed." The first hand card to announce **multiple graveyard cards** (`target_ids`, validated against `get_graveyard_search_candidates`) — Ancestral Spirit's reanimate announces one (`target_id`) and Ophelia's power is an activated power, so `_ability_removes_from_graveyard` keys on `dest == "rfg"` while explicitly excluding defs with an `activated_power` segment. **"Any number" includes zero**: unlike the reanimate branch an empty candidate pool does NOT make the card unplayable, and the browser still opens so the player can see there is nothing to exile. Each announced card is re-checked at resolution (706-style — still in a graveyard) and cards go to their OWNER's RFG zone (415.7a), so the heal counts what was ACTUALLY removed, not what was announced: recovering a target in response shrinks the heal. `requires_hero_race:Undead` is deck legality only (100.2b). AI (`BaseAI._graveyard_exile_action`): held while the hero is undamaged (denial alone isn't worth a card), then exiles EVERY ally card in the opponent's graveyard and tops up from its own only while the heal still does work |
| `activated_power:...:rfg_graveyard_ally:...:graveyard_ally:...` + `graveyard_to_rfg:Ally:1:1:both` | Graveyard-card-targeted ally power (Ophelia Barrows `azeroth_253`): "1 → Remove target ally card in any graveyard from the game. If you do, [she] heals 1 damage from herself." The `TARGETS` kind `graveyard_ally` makes `_can_use_ally_power` validate the target against the card's paired `graveyard_to_rfg` requirement segment (candidates must exist even for the `_skip_target_check` highlight probe). Resolution re-checks the target is still in a graveyard (fizzles otherwise), exiles it to its owner's RFG zone, and heals AMOUNT from the source only when the removal happened ("if you do"). UI: the graveyard browser (quest/Sen'zir machinery — `start_ally_graveyard_selection`, `_gy_select_ally_id`). AI: exiles only from the OPPONENT's graveyard, when the source is damaged or the exiled cost ≥ 3 |
| `on_combat_damage_to_hero:EFFECT[:N]` | Triggered attacker power (rule 703): "When [this] deals combat damage to a defending hero, …" Checked at combat conclusion (`_fire_combat_damage_to_hero_triggers`, right after the whelp-bounce check) — fires only when ≥1 combat damage actually LANDED on a defending hero (armor DEF/block absorbing all → no trigger; hitting a protecting ally → no trigger; a hero that protects IS the defender, so that counts). Still fires if the attacker died to a defensive weapon strike. `EFFECT` `draw` draws N for the attacker's controller (Randipan `azeroth_213`); `discard_controller` makes the damaged hero's controller discard N via the standard pending-discard machinery, no-op on an empty hand (Samuel Grey `azeroth_258`). No AI modeling — both are played as vanilla 2/2s |
| `hero_combat_dmg_destroys_ally` | Triggered equipment power (Devilsaur Leggings `azeroth_284`, Armor—Leather, Legs, DEF 1): "When your hero deals combat damage to an ally, destroy that ally." Paired with an `equipment:legs:1` segment. Fires at combat conclusion (`_fire_hero_combat_dmg_destroys_ally`, after the hero-damage triggers) when the wielder's HERO dealt ≥1 combat damage to an ally — as the attacker hitting an ally defender, OR as a protecting/directly-attacked defender retaliating with a struck weapon onto an attacking ally. Mandatory (no cost/choice — not a decision point); a no-op if the ally already died to the combat damage (the card matters when the ally SURVIVES). The trigger is ongoing/always-on: it fires even if the leggings were exhausted for armor prevention this combat (`_hero_wields_flag` doesn't check ready state). No AI modeling — played as a vanilla DEF 1 armor |
| `on_combat_damage_destroys_damaged_ally` | Triggered **ally** power (Brigg `azeroth_231`, 1-cost 1/2 Horde Orc Warrior): "When Brigg deals combat damage to an ally with damage on it, destroy that ally." Devilsaur Leggings' conclusion trigger with the source being the ally ITSELF (`_card_has_flag` reads the card's own def, vs `_hero_wields_flag`'s "hero whose controller has the equipment"), gated on damage the victim ALREADY carried. **"With damage on it" means pre-existing damage — Brigg's own combat damage does not qualify the target**; `_do_combat_conclusion` samples `damage_taken > 0` on both combatants in its existing "captured BEFORE damage lands" block, because after the packets every ally Brigg hit would trivially satisfy the clause and it would mean nothing. See `data/rules_deviations.md` "Brigg" for the reasoning. Mandatory (no cost/choice) and a no-op when the victim died to the combat damage anyway — it matters when a damaged ally SURVIVES. Fires in BOTH combat roles ("deals combat damage", not "attacks"): Brigg attacking a damaged ally defender, and Brigg defending and retaliating onto a damaged attacking ally. AI: modeled, unlike the other conclusion triggers — `BaseAI.combat_kills` is the new shared "does this combat remove that card?" predicate behind BOTH `find_safe_lethals` and `combat_trade_value`, so a damaged ally within Brigg's reach counts as a kill in every combat decision on offence and defence. Without it Brigg reads as a 1-ATK chump and the power would never be used on purpose |
| `hero_combat_dmg_locks_ally_ready` | Triggered weapon power (Iceblade Hacker `azeroth_328`, Weapon—Axe, Melee (1), 2 ATK / strike 2): "When your hero deals combat damage to an ally, that ally can't ready during its controller's next ready step." Paired with an `equipment:melee_weapon:0\|strike_cost:2` weapon segment. Same conclusion trigger point and cases as `hero_combat_dmg_destroys_ally` (`_fire_hero_combat_dmg_locks_ally_ready`, right after it), fired when the wielder's HERO dealt ≥1 combat damage to a surviving ally — sets Gouge's one-shot `gouge_skip_ready` counter on that ally (consumed at its controller's next ready step in `TurnManager._enter_ready`; drives the ⛓ badge) + emits `card_ready_locked`. Mandatory (no cost/choice); meaningful only when the ally SURVIVES (a dead ally left play). No AI modeling — played as a vanilla weapon |
| `berserk_counter_on_hero_damage` + `berserk_atk_on_hero_attack:N` | Berserking (`dark_portal_134`, 3, Ability — Horde, Troll Hero Required): "Ongoing: When your hero is dealt damage, put a berserk counter on Berserking. When your hero attacks, remove all berserk counters from Berserking. Your hero has +1 ATK this combat for each counter you removed." Ongoing Ability living in the hero_row (paired with `ongoing` + `requires_hero_race:Troll`). **One counter per damage EVENT**, not per point (the card says "is dealt damage") — hooked in `GameLogic.deal_damage` (`_add_berserk_counters`) after the packet lands, so armor prevention that reduces a hit to 0 adds no counter and `put_damage` (405.3, a self-damage cost) never triggers it. The attack half fires inline as the combat step starts (`_fire_berserking_on_attack` in `_resolve_propose_combat`, right after `combat_started`, BEFORE the strike point / attack window) — no cost, no choice, so it doesn't use the chain: it erases the counters and adds `per × removed` to `GameState.combat_atk_bonus[hero]`, a live "+N ATK this combat" grant read in `get_atk` (3c) and cleared with `combat_struck_weapons` at conclusion (both the normal and cancelled branches). Pending counters also feed `get_atk(..., assume_attacking)` via `_pending_berserk_atk` (3d), so a 0-ATK hero holding counters is a legal attacker and the attack cursor forecasts the boost — no double-count, since the trigger erases the counters as it fills the bonus. Stacks across copies. UI: the count shows on the card as an orange badge in the ATK-badge corner (`CardNode.update_counter`, refreshed from playtest `_refresh_atk_badges`). No AI modeling |
| `party_ready_on_attack:COST` | Party-wide version of `ready_on_attack:COST` (Windfury Totem `azeroth_118`, an Air Totem): "When each hero or ally in your party attacks for the first time each turn, you may pay COST. If you do, ready that character." `_open_ready_on_attack_point` falls back to `_party_ready_on_attack_cost` (lowest such flag among the controller's in-play cards) when the attacker has no OWN `ready_on_attack`. Both sources share the ONE `attacked_this_turn` gate — a character covered by both (Windseer Tarus under a Windfury Totem) still readies at most once per turn (attack + ready once, never three attacks over). Read live; grant lifts when the totem leaves play |
| `ready_on_strike:COST` | Ongoing attachment trigger (Windfury Weapon `azeroth_119`, paired with `attach:melee_weapon`): "When you strike with attached weapon for the first time each turn, you may pay COST. If you do, ready that weapon and your hero." Opened inside `choose_strike` after a successful first strike this turn with that weapon (`_open_strike_ready_point` → `pending_strike_ready_*` → `choose_ready_on_strike`, direct call; `can_submit`/`pass_priority` hard-block; the held combat window opens after). Gate: `windfury_struck_this_turn` weapon counter (cleared at turn ready step). AI (`BaseAI.choose_ready_on_strike`) pays only while ATTACKING. See `data/rules_deviations.md` "Windfury Weapon" |
| `attach:melee_weapon` | Attach-target kind restricting the attachment to a Melee weapon (Equipment with a `weapon` segment + `dmg_type` Melee) the caster controls (Windfury Weapon). `_attach_targets_weapon_only` / `_is_melee_weapon` gate submission (`_can_play_instant`/`_can_play_ability`), `_resolve_attach`, and the highlight probe (`_targeted_play_has_legal_target`). Targets picked from the caster's hero_row in `input_router._get_ability_targets`. AI: `_attach_actions` puts it on its own highest-ATK Melee weapon |
| `strike_cost:STRIKE_COST` | Marks an Equipment card as a weapon (rule 303) — always paired with an `equipment:SLOT:DEF` segment (slot e.g. `melee_weapon` for uniqueness, DEF 0). `STRIKE_COST` is the resource cost to strike (the number in the card's bottom-right circle — NOT the `Melee (1)` slot-uniqueness number on the type line). ATK comes from the CSV `atk` column, damage type from `dmg_type` ("Melee" gates the strike discount). See "Weapons & striking" section |
| `melee_strike_discount:N` | Hero flip power (Gorebelly): pay N less the next time you strike with a Melee weapon this turn (`PlayerState.melee_strike_discount`; consumed by the next melee strike, cleared at every turn start) |
| `two_handed` | Marks a weapon as Two-Handed (rule 414.3c): violates uniqueness with any `off_hand`-slot equipment, checked both ways in `_check_equipment_uniqueness` (same sacrifice choice as a slot conflict). Rod of the Ogre Magi |
| `power_weapon` | AI-only flag for "power weapons" — weapons whose value is their activated power, not the strike (Rod of the Ogre Magi, later Hypnotic Blade). The engine still offers the strike normally (a human may strike); the AI never strikes with one (`choose_strike_weapon` filters them out) and excludes it from `forecast_atk` (so it never proposes a hero attack on its account). Pair with an `activated_power` segment the AI already plays |
| `elusive` | (keyword in keywords col, not effects) — can't be chosen as defender |
| `protector` | (keyword) — can intercept attacks |
| `ferocity` | (keyword) — no summoning sickness |
| `stealth` | (keyword) — 602.2a: while this character is attacking, characters can't protect (`get_legal_protectors` gate) |
| `ranged` | (keyword) — long-range, defender can't deal combat damage back |
| `untargetable` | (keyword) — can't be chosen as a target of links (`_is_legal_target` in stack_resolver.gd, checked at submission AND resolution); combat/AoE unaffected. Chain Lightning's 2nd/3rd targets — and ALL three of Multi-Shot's targets — are a card-specific exception (allow flag) |

**`data_status`**: `verified` = card text confirmed from image  
**`engine_status`**: `implemented` = effects string is live in engine

### Tokens (`data/tokens.csv`)

Cards created by an effect rather than dealt from a deck (Mya's Mechanical
Dragonling, Tooga's Quest's Tooga). They live in their **own CSV** with the
same columns as `cards.csv`, and are loaded into the **same `CardDatabase`** —
every lookup in the engine, renderer and AI resolves a card by id, so tokens
must share the database. `CardDatabase.load_all()` loads both files and is the
only thing call sites should use (`load_csv(path, as_tokens)` is its plumbing);
defs from `tokens.csv` get `CardDef.is_token = true`.

- **Ids** are `token_<slug>` (no collector number). Instances are minted at
  runtime as `<controller>_token_<n>` off `GameState.token_counter`.
- **Not deckable** — `DeckManager.authorize_deck_def` rejects any `is_token`
  def alongside the rule 100.1 hero check. Without that gate a token would look
  legal in a deck list purely because it resolves in the database.
- **A token ceases to exist when it leaves play.** The redirect lives in
  `GameLogic.move_card`: ANY play → non-play move of a card with
  `CardInstance.is_token` goes to the owner's RFG zone instead. One branch
  covers destroy, bounce (Withdraw), discard and every future removal by
  construction, and no token can ever reach a graveyard for recursion
  (Ophelia, Augustus) to find. It changes only the DESTINATION — `destroy_card`
  still emits `card_destroyed` first, so `on_destroyed` triggers and "when an
  ally is destroyed" watchers see a token exactly as a real card.
  See `data/rules_deviations.md` "Tokens".
- **Renderer:** a token has never been in a zone, so no CardNode exists for it —
  `playtest.gd`'s `card_moved` handler spawns one as it enters the ally_row
  (same pattern as the drawn-card branch). Art of any resolution renders at card
  size for free: `CardNode`'s TextureRect is a fixed 75×105 with
  `STRETCH_SCALE` + `EXPAND_IGNORE_SIZE`, so the source pixel size is ignored.
  Aspect ratio is NOT preserved — a source far off 5:7 would be stretched.
- Adding a token: drop the art in `assets/cards/tokens/`, add a row to
  `data/tokens.csv` (`engine_status=implemented`, `image_path` set), and run the
  Godot importer once so the image has a `.import` file — `load()` returns null
  without it.

**CSV field quoting:** always quote `power_text` and `card_subtype` values to safely handle special characters (commas, quotes, line breaks). Use standard CSV quoting: wrap the field in double quotes and escape internal quotes as `""`. For example:
```
azeroth_56, Frostbolt, "Instant Ability — Frost", "Deal 3 frost damage to target hero or ally. That hero or ally can't attack this turn."
```

**Engine behavior that deviates from the printed rules/card text** (e.g. a
timing restriction not in the printed text but true by construction of the
duel format) must be tracked in `data/rules_deviations.md`, with a code
comment at the enforcement site pointing back to it. Don't hardcode a
card-specific check in `stack_resolver.gd` for this — add a data-driven
effects flag instead (see `require_turn_player` for the pattern).

---

## Card uniqueness

WoW TCG allies are **NOT unique** by default. Do not assume uniqueness unless the card text says so or the card is a **Pet** (Pet uniqueness is enforced separately).

**Pet uniqueness (rule 414.3b):** A player may control at most `PlayerState.pet_capacity` pets (default 1) in their ally_row simultaneously. When a second Pet enters play, `_check_pet_uniqueness` fires immediately (non-interruptible), setting `pending_pet_sacrifice_player` and emitting `pet_sacrifice_required`. The player must call `StackResolver.choose_pet_sacrifice()` directly (not via `submit_action`).

Pet capacity is a player attribute (`PlayerState.pet_capacity`, default 1) that card effects can increase.

**Name-based uniqueness (rule 414.3a — the "Unique" tag):** cards whose `keywords` column contains `Unique` (parsed to `"unique"`). A player may not control two or more in-play cards that share a name and both carry Unique (Lady Jaina Proudmoore). `_check_unique_uniqueness` fires after a card enters play (called from `_resolve_play_ally` and `_resolve_play_equipment`); on violation it sets `pending_unique_sacrifice_player`/`_ids` and emits `unique_sacrifice_required`. Resolve via `StackResolver.choose_unique_sacrifice()` (direct call, not the chain — same pattern as pet/equipment sacrifice; `can_submit` hard-blocks while pending). UI mirrors the equipment-sacrifice mode (red highlight, pass button shows "Destroy a duplicate"); AI keeps the first copy.

---

## Highlight color conventions (input_router.gd → board_renderer.gd)

- **Green** (`Color(0.2, 1.0, 0.3)`) — default, normal playable cards / targeting
- **Red** (`Color(1.0, 0.25, 0.25)`) — mandatory choice: discard (`start_discard_mode`) or pet sacrifice (`start_pet_sacrifice_mode`)

`refresh_highlights()` emits `highlights_updated(ids, color)`. The color is stored as `_highlight_color` in `InputRouter` and reset to green when the mandatory mode ends.

---

## AI conventions (game_logic/ai/base_ai.gd)

- `get_reasonable_actions` only returns actions for the **turn player** (non-turn player returns empty — AI passes during opponent's priority windows, including combat windows).
- **Targeting rules:** AI always targets **opponents only**. Never self-target for damage/destroy effects. This applies to:
  - `_targeted_instant_actions` (Vanquish, etc.) — only `opp + "_ally_row"` and opp hero
  - `_handle_enter_play_target` in `playtest.gd` — same: `opp` only
  - `_get_ally_power_actions` — already enemy-only
- `_destroy_is_worth_it(state, db, player_id, target_id, spell_cost)` — AI only plays destroy spells when target cost ≥ spell cost AND no friendly ally can solo-kill the target in combat.
- **Combat instants (held cards / ambush):** `BaseAI.COMBAT_INSTANT_TAGS` tags cards (by `card_def_id`) the AI must HOLD — `get_reasonable_actions` never blind-plays them; `combat_instant_action()` plays them only when the AI is **being attacked**, during attack/defend windows (see `ai_functions.md` for the exact math). Quick Strike (`azeroth_165`) is tagged `combat_instant_dmg`. The AI announces `target_id = state.combat_attacker` at submission. All AI classes inherit the behavior (BaseAI.decide_action; FullRandomAI checks it before random play).
- **Targeted instants announce their target at play time** (`_instant_needs_target` covers `destroy_target` AND `deal_damage_to_target`) — humans get the standard cancellable (Esc) targeting flow via `start_targeting`, highlighted with the looser `can_play_instant_no_target_check` probe. There is no deferred target pick for instants.

---

## Deck system (spec §9 — implemented)

Deck lists live as JSON files on disk, NOT in playtest.gd. Playtest only picks deck ids.

- `decks/base/`, `decks/recommended_ai/`, `decks/custom/` — one JSON per deck (`deck_id` = filename stem).
  - `recommended_ai/` (6 decks): simple-heuristic-friendly decks AI plays reasonably well — Ta'zo, Grennan, Omedus, Timmo, Boris, Sen'zir.
  - `base/` (3 decks): Dizdemona, Radak, Moonshadow — decks that need situational judgment beyond what FullRandomAI/BaseAI handle well, excluded from "Recommended for AI". Still human-playable and findable under "All decks" — can be slotted onto an AI player, just expect weaker AI performance. `recommended_ai_id` is still set (`ai_generic`), the correct AI to use if one is forced onto the deck.
- `ai_profiles/*.json` — `AIProfile` (`ai_id`, `ai_class`: "base"|"fullrandom"|"generic", `strategy_data`). All 8 demo decks specify `ai_generic` (GenericAI — see `game_logic/ai/ai_roster.md` and `ai_functions.md`).
- Classes: `DeckDefinition`/`DeckCardEntry` (game_logic/deck_definition.gd, deck_card_entry.gd), `DeckLibrary`/`DeckLibraryIndex` (scan/categorize, ids only), `DeckManager` (single entry point: `get_available_decks()`, `load_deck()`, `validate_deck()` [structural: hero + ≥60 cards], `authorize_deck()` [see below], `get_runtime_deck()` → `Deck`, `load_ai_profile()`, `make_ai_for_deck()`).
- **Deck authorizer (rule 100 legality):** `DeckManager.authorize_deck(deck_id, db)` / `authorize_deck_def(deck, db)` → error strings ([] = legal). Checks every card id resolves in the CardDatabase (catches unknown AND not-yet-implemented ids — the db only loads `engine_status=implemented`), no Hero cards among the 60 (100.1), max 4 copies per name (100.4), faction icons match the hero, and class icons share a class with the hero (100.2a). Class restriction is enforced ONLY on Abilities/Equipment — an ally's class column is its flavor subtype, never a deckbuilding restriction. **Talents (rule 100.2c):** a card whose tags column ends in `" Talent"` (`DeckManager.talent_spec`, e.g. Aimed Shot "Marksmanship Talent", Spirit Bond "Beast Mastery Talent") — since our heroes list no talent spec (206.1), the authorizer rejects a deck mixing Talents of two different specs; a Talent plus non-Talent cards of any subtype is fine. If heroes ever gain a spec trait, the hero-match branch of 100.2c goes in `authorize_deck_def`. Multi-class cards use concatenated two-letter abbrevs in the class column (`MaPrLo`; Lo=Warlock — `DeckManager.CLASS_ABBREVS`). Called by playtest `_launch_game` before the menu is torn down, so a hand-edited custom deck fails with readable menu errors instead of breaking mid-game; `test_deck_manager.gd` authorizes every shipped deck (this replaces composition-pinning asserts).
- **Nothing except DeckManager reads deck files or calls DeckLibrary.** Playtest menu has a category dropdown per player ("All decks" vs "Recommended for AI") that filters the deck dropdown via `get_available_decks()`; "Recommended AI" player type uses `make_ai_for_deck()`. An "Avoid mirror matches" checkbox (default on) excludes the other player's fixed deck from a random pick, or refuses to start on a fixed mirror.
- Tests: `game_logic/tests/test_deck_manager.gd`.

Demo decks (all 60 cards): Dizdemona (azeroth_2, base), Ta'zo (azeroth_15, recommended_ai), Grennan (azeroth_10, recommended_ai), Boris (azeroth_1, recommended_ai), Omedus (azeroth_12, recommended_ai), Radak (azeroth_13, base), Timmo (azeroth_7, recommended_ai), Moonshadow (azeroth_6, base), Sen'zir (azeroth_14, recommended_ai).

---

## Currently implemented cards (engine_status = implemented)

Notable implemented mechanics:
- **Elusive** (keyword): can't be chosen as defender
- **Protector** (keyword): can intercept attacks
- **Ferocity** (keyword): no summoning sickness
- **Stealth** (keyword): while this character is attacking, characters can't protect (rule 602.2a)
- **Ranged** (keyword): defender can't deal combat damage back (Long-Range)
- **Untargetable** (keyword): can't be chosen as a target of links (rule 706); combat and non-targeted effects (AoE) unaffected. A target that becomes Untargetable after the announce fizzles at resolution (glossary 4217)
- **on_enter:deal_damage_to_target** — Taz'dingo-style enter-play targeted effects
- **on_enter:destroy_exhausted_damaged_ally** — Ghank (`dark_portal_210`, 3/3 Stealth Orc Rogue): "When Ghank enters play, you may destroy target exhausted ally with damage on it." First **optional** targeted enter-play trigger — the choice only opens when a legal target exists (`get_enter_play_destroy_targets`: an ally, either party, both exhausted AND `damage_taken > 0`, 706-legal); no target → the trigger silently doesn't fire, no pop-up. Declinable via `StackResolver.decline_enter_play_effect` (`optional` flag on `pending_enter_play_effect`; Esc for human, AI declines unless an OPPOSING ally is a target — never self-harm). Resolution re-checks exhausted+damaged+legal (706)
- **activated_power** — Grimdron (1 fire dmg to hero or ally, usable during combat windows)
- **sarmoth_taunt** — Sarmoth (opposing characters that can attack Sarmoth must only target Sarmoth)
- **destroy_target:ally** — Vanquish
- **Pet uniqueness** — pet_capacity enforced with immediate sacrifice choice
- **Infernal** (azeroth_127) — start-of-turn "discard or give control" choice (declinable, unlike a forced discard) + end-of-turn 1 fire to each opposing hero and ally
- **Equipment** — Mooncloth Robe (first equipment). Plays via `play_equipment` into the hero row; `activated_power` with `draw` effect + `exhaust_hero` extra cost. Slot uniqueness (rule 414.3) enforced with an immediate destroy choice mirroring Pet uniqueness (`pending_equip_sacrifice_*`, `choose_equipment_sacrifice`, `equipment_sacrifice_required`). See "Equipment" section below.
- **Green Whelp Armor** (azeroth_291) — first *triggered* equipment power: when an attacking ally deals combat damage to your hero, you may pay 2 to bounce it to its owner's hand (`whelp_bounce`; `_maybe_open_whelp_bounce` at combat conclusion → direct-call `choose_whelp_bounce`)
- **Litori Frostburn** (azeroth_5) — `target_cant_attack` flip: cancels an enemy combat proposal on the chain via the 601.3 recheck (attacker doesn't exhaust); too late once the attack window is open. First "instant save that doesn't kill the attacker" AI behavior (`BaseAI.hero_disable_action`)
- **Hero powers**: Ta'zo flip (deal_damage_to_target:3:fire), Dizdemona (deal_x_damage_to_ally), Omedus (deal_damage_aoe), Grennan (heal), Boris Brightbeard (heal_x_from_target — pay X resources, heal X from any hero or ally), Radak Doombringer (radak_pet_sacrifice — flip: sacrifice Pet with cost X, deal X shadow dmg to target)
- **Quest cards** — basic cost-based quest completion
- **Reveal-and-pick quests** — Big Game Hunter (`azeroth_348`, Equipment:4), Kibler's Exotic Pets (`azeroth_355`, Ally:3), Zapped Giants (`azeroth_361`, Ability:3): reveal top N, keep one revealed card of a type, rest to bottom (`reveal_pick` recipe; human picks via the reveal browser, AI keeps highest-cost via `_pick_ai_reveal`)
- **The Princess Trapped** (`azeroth_357`, Quest, pay 2) — "Reveal the top two cards of your deck. Target opponent chooses one. Put that card into your hand and the other one on the bottom of your deck" (`reveal_pick:Any:2:opponent`). First card whose **decider differs from its owner**, which is what introduced `pending_reveal_pick_chooser` and the scene's `_route_choice` funnel (see the effects-table row and the Hotseat section).
- **It's a Secret to Everybody** (`azeroth_354`, Quest, pay 1) — "Look at the top three cards of your deck. Put one on the top of your deck and the rest on the bottom" (`reveal_pick:Any:3:to_top:private`). First reveal-pick whose destination is the **top of the deck** rather than the hand, and the first private "look at" (routed `_route_choice(chooser, "private")`; the shared log doesn't name the kept card) — see the `reveal_pick:Any:N:to_top:private` effects-table row.
- **Choose-one-or-both quests** (`qmode:` framework) — **Hidden Enemies** (`dark_portal_302`, Horde, pay 3: target ally has ferocity this turn / draw; Orc → both), **A New Plague** (`dark_portal_304`, Horde, pay 4 during your turn: each player destroys an ally in his party / draw; Undead → both), **Thwarting Kolkar Aggression** (`dark_portal_309`, Horde, pay 3 during your turn: opponent turns a quest face down / draw; Troll → both), **Crown of the Earth** (`dark_portal_289`, Alliance, pay 3: hand to bottom of deck + redraw / draw; Night Elf → both). First modal quest rewards, first buff-granted keyword (ferocity), first quest-flip effect. See the `qmode:` effects-table row.
- **The Love Potion** (`azeroth_356`, Quest, cost 1) — "Exhaust two allies in your party and pay (1) to complete this quest. Reward: Draw a card" (`exhaust_allies:2|draw:1`). First quest with a **non-resource completion cost**, and the first pre-submission (cancellable, zero-state-change) cost picker in the UI — see the `exhaust_allies` effects-table row.
- **Totem** (rule 305.3) — Searing Totem (`azeroth_116`, Instant Ability—Elemental, Fire Totem, 0/1). Ability ally that enters the ally_row, can't attack, can be attacked/targeted like an ally. Ongoing power deals 1 fire damage to a chosen hero/ally at the start of each turn (`totem:fire` + `ongoing_damage_each_turn:1:fire`; direct-call `choose_totem_target` picks the target, then the trigger goes on the chain with a priority window before the damage — rule 501.1a/410). First card of the general Totem framework.
- **Earthbind Totem** (`azeroth_107`, 2, Instant Ability—Elemental, Earth Totem (1), 0/1) — "Ongoing: Opposing allies can't ready during their controllers' ready step" (`ongoing|totem:earth|opposing_allies_cant_ready`). First ready-lock aura; ready-blocked cards show the ⛓ badge.
- **Healing Stream Totem** (`azeroth_111`, 1, Instant Ability—Restoration, Water Totem (1), 0/1) — "Ongoing: At the start of each turn, [this] heals 1 damage from each hero and ally in your party" (`ongoing|totem:water|heal_party_each_turn:1`).
- **Windfury Totem** (`azeroth_118`, 4, Instant Ability—Enhancement, Air Totem (1), 0/1) — "Ongoing: When each hero or ally in your party attacks for the first time each turn, you may pay (1). If you do, ready that character" (`ongoing|totem:air|party_ready_on_attack:1`). Party-wide `ready_on_attack`; shares the once-per-turn `attacked_this_turn` gate — see the `party_ready_on_attack` effects-table row.
- **Windfury Weapon** (`azeroth_119`, 2, Instant Ability—Enhancement) — "Attach to one of your Melee weapons. Ongoing: When you strike with attached weapon for the first time each turn, you may pay (1). If you do, ready that weapon and your hero" (`ongoing|attach:melee_weapon|ready_on_strike:1`). First attach-to-weapon and first ready-on-strike point — see the `attach:melee_weapon` / `ready_on_strike` effects-table rows and `data/rules_deviations.md` "Windfury Weapon".
- **Long-Range** (keyword) — Tanwa the Marksman (dark_portal_235, 4/3). While a Long-Range character is attacking, the defender deals no combat damage back (`_do_combat_conclusion` zeroes `def_dmg` when the attacker has `long_range`; has no effect when the character is defending instead).
- **Weapons & striking** — Krol Blade (azeroth_331, first weapon) + Gorebelly (azeroth_9, melee strike discount flip). See "Weapons & striking" section
- **Frostbolt / Frost Shock** (`azeroth_56`, 3 frost; `azeroth_109`, 2 frost) — Instant Abilities that deal frost damage to a target hero/ally, then apply a "can't attack" (Frostbolt) or "can't attack or protect" (Frost Shock) restriction to a surviving target (`deal_damage_to_target` 4th field → `_apply_damage_riders`). Tagged `combat_instant_dmg` like Quick Strike; AI plays them for the damage only.
- **Steal Essence** (`azeroth_134`, 2, Instant Ability — Affliction, Warlock) — "Your hero deals 2 shadow damage to target hero or ally and heals 1 damage from itself for each damage dealt" (`deal_damage_to_target:2:shadow|drain_heal_per_damage:1`). First drain effect — see the `drain_heal_per_damage` effects-table row. Tagged `combat_instant_dmg`; AI plays it for the damage, drain not modeled.
- **Mind Spike / Mind Blast** (`azeroth_82`, 2, 1 shadow; `azeroth_80`, 5, 2 shadow — Ability — Shadow, Priest) — "Your hero deals N shadow damage to target hero or ally. Its controller discards a card for each damage dealt" (`deal_damage_to_target:N:shadow|discard_per_damage:1`). Sorcery-speed (plain Ability) targeted damage + the damaged character's controller discards one card per damage dealt. First `discard_per_damage` cards — see the effects-table row.
- **Dark Cleric Ismantal** (`dark_portal_204`, 3-cost 1/3 Horde Undead Priest Ally) — "4, [Activate]: deals 1 shadow damage to target hero or ally. That character's controller discards a card for each damage dealt. Use only on your turn" (`activated_power:4:deal_damage_to_target:1:shadow:hero_or_ally|discard_per_damage:1|on_your_turn`). The `discard_per_damage` rider on an ally activated power; reuses the ally-power damage path.
- **Boneshanks** (`dark_portal_201`, 3-cost 3/2 Horde Undead Warrior Ally) — "When Boneshanks is destroyed, destroy target ally" (`on_destroyed:destroy_target:ally`). First **targeted death trigger**: a mandatory direct-call choice (`pending_death_triggers` queue → `choose_death_target`) opened for the destroyed card's controller, prompted even in hotseat. See the `on_destroyed` effects-table row.
- **Purge** (`azeroth_114`, 1, Instant Ability — Elemental, Shaman) — "Destroy target ability unless it's attached to a friendly hero or ally" (`destroy_target:ability|except_friendly_attached`). Instant-speed Burn Away minus the caster's own characters' attachments — see the `except_friendly_attached` effects-table row.
- **Multi-Shot** (`azeroth_41`, 5, Instant Ability — Marksmanship) — "Your hero deals 2 ranged damage to each of up to three target heroes and/or allies" (`multi_shot:2:ranged`). Second multi-target announced-in-order spell after Chain Lightning; reuses its target-picking UI/AI machinery (`_is_multi_target`), but is instant-speed, deals a flat amount to every target, and lets **all three** slots select Untargetable characters.
- **Natural Selection** (`azeroth_27`, 3, Instant Ability — Restoration, Druid) — "Choose one: Your hero deals 3 nature damage to target hero or ally; or your hero heals 3 damage from target hero or ally" (`mode:deal_damage_to_target:3:nature|mode:heal_target:3`). First **modal** link (rule 707.1c) — see the `mode:` effects-table row.
- **Ravenous Bite** (`azeroth_44`, 2, Instant Ability — Beast Mastery, Hunter) — "Target ally has +3 ATK this turn. Target ally has -3 ATK this turn" (`atk_swing:3:-3`). First two-target ATK swing and first negative ATK modifier (get_atk floors at 0). Read literally as BOTH effects, not a modal choice — the printed text lacks the "Choose one —" prefix every modal card carries (Natural Selection). See the `atk_swing` effects-table row.
- **Shock and Soothe** (`dark_portal_100`, 4, Instant Ability — Elemental, Shaman) — "Your hero deals 3 nature damage to target hero or ally and heals 3 damage from another target hero or ally" (`deal_damage_and_heal:3:nature:3`). First **hand card** on the `deal_damage_and_heal` key (previously hero-power / ally-power only) — two mandatory, distinct hero-or-ally targets. See the effects-table row.
- **Exhaustion** (`azeroth_159`, 2, Instant Ability) — "Exhaust target ally" (`exhaust_target:ally`). A held combat instant the AI plays on an opposing **ally** attacker while its combat proposal is on the chain; exhausting it fizzles the proposal (601.3), the same interrupt/AI role as Litori's freeze but as an affordable hand card. Ally-only targeting; too late once the attack window opens. See the `exhaust_target` effects-table row.
- **Gouge** (`azeroth_99`, 1, Instant Ability — Combat Combo, Rogue) — "Exhaust target hero or ally. It can't ready during its controller's next ready step" (`exhaust_target:hero_or_ally|gouge_cant_ready`). Exhaustion/Bash's freeze plus a one-shot ready-lock (a `gouge_skip_ready` counter consumed at the target controller's next ready step). Tagged `combat_instant_exhaust` — the AI answers an opposing attacker (hero or ally). See the `gouge_cant_ready` effects-table row.
- **Keyword grants** (`grant_keyword_target:KIND:KEYWORD`) — general framework for "target ally has KEYWORD this turn", reusing the existing target kinds and the Hidden Enemies `grant_<keyword>` Buff (see the effects-table row). First cards: **Sneak** (`azeroth_152`, 1, Instant Ability, Alliance): "Target ally has elusive this turn" (`grant_keyword_target:ally:elusive`) — a combat save, cast on our own proposed defender so the 601.3 recheck fizzles the proposal without the attacker exhausting (`BaseAI.elusive_save_action`, tagged `combat_instant_save_elusive`). **Into the Fray** (`azeroth_153`, 1, Ability, Horde): "Target ally in your party has ferocity this turn" (`grant_keyword_target:friendly_ally:ferocity`) — sorcery-speed, friendly-only; the AI aims it only at summoning-sick allies with ATK > 0.
- **Brigg** (`azeroth_231`, 1-cost 1/2 Horde Orc Warrior Ally) — "When Brigg deals combat damage to an ally with damage on it, destroy that ally" (`on_combat_damage_destroys_damaged_ally`). First triggered conclusion power sourced from the ALLY itself rather than the hero's equipment, and the first one the AI models: `BaseAI.combat_kills` now backs both `find_safe_lethals` and `combat_trade_value`. "With damage on it" is read as pre-existing damage — see the effects-table row and `data/rules_deviations.md` "Brigg".
- **Board-wide keyword auras** (`all_allies_keyword:KEYWORD`) — general framework for "Ongoing: All allies have KEYWORD", read live inside `_has_keyword` (see the effects-table row; this is what added the optional `state` param there). First cards: **Lust for Battle** (`azeroth_154`, 4, Ability, Horde): "Ongoing: All allies have ferocity" (`ongoing|all_allies_keyword:ferocity`). **From the Shadows** (`azeroth_151`, 5, Ability, Alliance): "Ongoing: All allies have elusive" (`ongoing|all_allies_keyword:elusive`) — with it out, no ally on either board can be proposed as a defender, so every attack goes at a hero (Protectors still protect). Both are symmetric: they grant to the OPPONENT's allies as well, and to allies that arrive after the aura.
- **Coup de Grâce** (`azeroth_93`, 2, Ability — Assassination, Rogue) — "Destroy target exhausted ally" (`destroy_target:exhausted_ally`). Sorcery-speed removal restricted to exhausted allies of either party; fizzles if the target readies before resolution. AI destroys the most valuable exhausted opposing ally. See the `destroy_target:exhausted_ally` effects-table row.
- **Trophy Kill** (`dark_portal_40`, 3, Instant Ability — Marksmanship, Hunter) / **Prey on the Weak** (`dark_portal_85`, 2, Instant Ability — Assassination, Rogue) — "Destroy target ally with cost 4 or more" / "…cost 4 or less" (`destroy_target:ally|target_cost_min:4` / `|target_cost_max:4`). Mirror cards banded on the target's **printed** cost; both bands are inclusive, so a cost-4 ally is legal for either. First cost-restricted removal — see the `target_cost_min` / `target_cost_max` effects-table row. Test: `_test_cost_banded_destroy`.
- **Blink** (`azeroth_48`, 2, Instant Ability — Arcane, Mage) — "Draw a card. If your hero is defending, remove all attackers from combat" (`draw:1|remove_attackers:hero_defending`). First remove-from-combat effect — see the `remove_attackers` effects-table row.
- **Withdraw** (`azeroth_172`, 3, Instant Ability) — "Put target ally into its owner's hand" (`return_to_hand:ally`). First bounce effect; the counter-spell save play (bounce your own targeted ally, the opposing removal fizzles per 709.2a). See the `return_to_hand` effects-table row.
- **War Stomp** (`dark_portal_137`, 3, Instant Ability, Horde, **Tauren Hero Required**) — "Exhaust all opposing heroes and allies" (`requires_hero_race:Tauren|exhaust_all_opposing`). First mass exhaust and first "[Race] Hero Required" deckbuilding restriction (rule 100.2b, enforced in the deck authorizer) — see both effects-table rows.
- **Berserking** (`dark_portal_134`, 3, Ability — Horde, **Troll Hero Required**) — "Ongoing: When your hero is dealt damage, put a berserk counter on Berserking. When your hero attacks, remove all berserk counters from Berserking. Your hero has +1 ATK this combat for each counter you removed" (`ongoing|requires_hero_race:Troll|berserk_counter_on_hero_damage|berserk_atk_on_hero_attack:1`). First card with **visible counters** (orange badge, ATK-badge corner) and the first "+N ATK this combat" grant (`GameState.combat_atk_bonus`) — see the effects-table row.
- **Tokens** (`data/tokens.csv`) — general framework: a separate CSV loaded into the same `CardDatabase`, non-deckable, and voided on any play → non-play move (see the Tokens section under Card data format). First cards: **Mya, Dragonling Wrangler** (`dark_portal_184`, 3-cost 2/2 Alliance Gnome Mage): "When Mya enters play, put a Mechanical Dragonling ally token with 1 ATK and 1 health into play" (`on_enter:create_token:token_mechanical_dragonling`) — first token creator. **Tooga's Quest** (`azeroth_359`, Quest, pay 3 during your turn): "Reward: Put a unique Turtle ally token named Tooga with 1 ATK and 1 health into play. At the start of your next turn, remove Tooga from the game. If you do, draw two cards" (`create_token:token_tooga|require_turn_player`, with `rfg_self_next_turn:draw:2` on the token) — first token from a quest reward and first delayed self-removal trigger. Tooga is Unique (414.3a). No AI modeling needed: Mya is played as a plain ally and the quest completion is generic.
- **Damage prevention & unpreventable damage** — general framework: `GameLogic.prevent` is the single application point (see the Armor damage prevention section), unpreventability is opt-in per packet and derived from the source, and prevention POINTS are suppressed for packets that can't be prevented. First cards: **Lionheart Helm** (`azeroth_297`, 4, Armor — Plate, Head (1), DEF 2): "Damage dealt by your hero can't be prevented" (`equipment:head:2|hero_damage_unpreventable`). **Annihilator** (`azeroth_312`, 2, Weapon — Axe, Melee (1), 3 ATK / strike 2): "Combat damage dealt by your hero with Annihilator can't be prevented" (`equipment:melee_weapon:0|strike_cost:2|combat_damage_unpreventable`). **Brother Rhone** (`dark_portal_157`, 2-cost 0/1 Alliance Dwarf Paladin, Protector): "Prevent all combat damage that would be dealt to Brother Rhone by attacking allies" (`prevent_combat_damage_from_attacking_allies`) — the first prevention shield that isn't armor. Tests: `_test_lionheart_helm_unpreventable`, `_test_annihilator_unpreventable`, `_test_brother_rhone_shield`.
- **Lady Jaina Proudmoore** (`azeroth_195`, 7/4 Ally, Unique) — "Opposing allies can't attack." Live static aura (`opposing_allies_cant_attack`) + first card exercising **name-based Unique uniqueness** (rule 414.3a — see Card uniqueness).
- **Bala Silentblade** (`azeroth_226`, 3-cost 1/4 Horde Orc Rogue) — "+3 ATK while attacking an exhausted hero or ally." Live conditional self-modifier (`atk_vs_exhausted_defender:3`), natural partner of the Chops/Voss attack-exhaust trigger. See the effects-table row.
- **Chops** (`dark_portal_32`, 3-cost 3/4 Boar, Pet) / **Voss Treebender** (`azeroth_266`, 1-cost 2/1 Horde Tauren Druid) — "When [this] attacks, you may exhaust target hero or ally." First attack-triggered targeted power (`on_attack_exhaust_target`): resolves before the attack window, so it can exhaust a ready Protector and deny the protect point. See the effects-table row.
- **Attachments** (rule 400) — general framework: `attach:TARGETS[:exhaust_it]` + host-side ongoing segments; shared `"attached"` zone; host leaving play destroys attachments (see the effects-table rows). First cards: **Entangling Roots** (`azeroth_20`, 2, Ability — Balance, Druid): attach to target ally and exhaust it; attached ally can't ready during its controller's ready step (`attach:ally:exhaust_it|attached_cannot_ready`). **Mark of the Wild** (`azeroth_24`, 2, Instant Ability — Restoration, Druid): attach to target ally; +2 ATK / +2 health (`attach:ally|attached_buff:2:2`).
- **Kavai the Wanderer** (`dark_portal_173`, 6-cost 4/6 Alliance Night Elf Warrior) — "1, Destroy Kavai → Destroy target ability or equipment." First `sacrifice_self` extra cost (plain payment power — no tap symbol, usable while exhausted / just summoned) and first `destroy_ability_or_equipment` effect (`TARGETS=ability_or_equipment`, hits ongoing abilities, totems, attachments, and equipment of either player). AI: never fired proactively — `BaseAI.doomed_sacrifice_action` (wired into all three AIs' `decide_action`, after `save_bounce_action`) cashes her in on the opponent's most expensive ability/equipment only when she is DOOMED: target of a lethal opposing removal/damage link on the chain (shared `_chain_threatened_ally` detection with Withdraw), or dying at the open combat window's conclusion (as defender, or as attacker to a lethal retaliation).
- **Lafiel** (`azeroth_196`, 6-cost 4/5 Alliance Night Elf Priest) — "2, [Activate] → Destroy target ability." Kavai's destroy pool narrowed to abilities only (`activated_power:2:destroy_ability:0::ability` — new `TARGETS` kind `ability`, sharing `get_destroy_kind_candidates`, so it hits ongoing abilities, totems and attachments of either player but never equipment), on a normal `[Activate]` tap power: she exhausts at announcement, so it can't be used again until she readies. No Purge-style `except_friendly_attached` exclusion — her printed text has no such clause, so the caster's own abilities stay legal targets. AI (`_get_ally_power_actions` `destroy_ability` branch): destroys the most expensive OPPOSING ability, never its own.
- **Moira Darkheart** (`azeroth_209`, 4-cost 3/4 Alliance Night Elf Rogue) — "1, Destroy Moira Darkheart → Destroy target armor or weapon." Kavai's `sacrifice_self` shape with the pool narrowed the other way from Lafiel (`activated_power:1:destroy_equipment:0::equipment:sacrifice_self` — new `TARGETS` kind `equipment`). "Armor or weapon" IS the equipment pool: every Equipment card is one or the other, which is what Zygore Bladebreaker's `include_weapons = true` already resolves to, so no armor/weapon split is needed. No `[Activate]` tap symbol — usable while exhausted / just summoned — and the sacrifice is paid at resolution, so a Moira killed in response no-ops the cost while the announced destroy still resolves. AI: `BaseAI.doomed_sacrifice_action` now derives its candidate kinds from the power's effect, so she is cashed in only when DOOMED and only onto opposing equipment.
- **Cannibalize** (`dark_portal_136`, 2, Ability — Horde, **Undead Hero Required**) — "Remove any number of ally cards in graveyards from the game. Your hero heals 2 damage from itself for each card removed" (`requires_hero_race:Undead|graveyard_to_rfg:Ally:0:99:both|rfg_heal_per_card:2`). First multi-card graveyard announce from a hand card, and the card that split the graveyard browser per graveyard — see the effects-table row and the Graveyard browser section.
- **Ophelia Barrows** (`azeroth_253`, 4-cost 1/5 Horde Undead Warrior, Protector) — "1 → Remove target ally card in any graveyard from the game. If you do, Ophelia Barrows heals 1 damage from herself." First graveyard-card-targeted activated power (`rfg_graveyard_ally` + paired `graveyard_to_rfg:Ally:1:1:both` requirement); plain payment power (`no_activate`), repeatable. See the effects-table row.
- **Randipan** (`azeroth_213`, 3-cost 2/2 Alliance Gnome Rogue) / **Samuel Grey** (`azeroth_258`, 3-cost 2/2 Horde Undead Rogue) — "When [this] deals combat damage to a defending hero, draw a card" / "…that hero's controller discards a card" (`on_combat_damage_to_hero:draw:1` / `:discard_controller:1`). See the effects-table row.
- **Forms** (rule 414.3b + glossary Bear/Cat Form) — general framework: `form:1` slot uniqueness, `form_break:Feral` break condition, `on_destroyed:pay_return_hand:2` optional return (see the effects-table rows). First cards: **Bear Form** (`azeroth_18`, 1, Instant Ability — Feral): hero has protector (`hero_has_protector`) + pay-2 return. **Bash** (`azeroth_17`, 2, Instant Ability — Feral): "Exhaust target hero or ally" on play (`exhaust_target:hero_or_ally` — first hero-capable exhaust) + bear form ongoing, NO return clause. **Cat Form** (`dark_portal_19`, 2, Instant Ability — Feral): hero +1 ATK while attacking (`hero_atk_while_attacking:1`) + pay-2 return. **Claw** (`dark_portal_20`, 4, Instant Ability — Feral Combo): hero deals 3 melee to target hero/ally on play + cat form ongoing, NO return clause. Bash/Claw exercise ongoing-ability **on-play effect segments** (resolved in `_resolve_play_ongoing_ability` before the card settles into the hero_row). AI: Bear Form tagged `combat_instant_bear_form` — `BaseAI.bear_form_action` (wired into all three AIs) flashes it in during the ATTACK window when attacked, hero ready, not already in bear form, and no board protector answers; Bash tagged `combat_instant_exhaust` (Exhaustion role, and unlike Exhaustion it also answers an attacking HERO); Claw tagged `combat_instant_dmg`; Cat Form untagged — played normally in GenericAI's develop step; `BaseAI.choose_form_return` always pays. **AI hero-attack policy:** `get_reasonable_actions` offers a hero attack on an enemy ALLY only when the forecast ATK is lethal on it (attacks on the enemy hero are always offered); GenericAI's kill/trade/chip steps already conform.
- **Fireball** (`azeroth_53`, 4, Ability — Fire, Mage) — "Attach to target hero or ally, and your hero deals 4 fire damage to it. Ongoing: At the start of your turn, your hero deals 1 fire damage to attached character." First hero-or-ally attachment (`attach:hero_or_ally|attach_deal_damage:4:fire|attached_damage_turn_start:1:fire`) — see the effects-table rows. AI targets the opposing hero only.
- **World in Flames** (`azeroth_61`, 8, Ability — Fire, Mage) — "Ongoing: If your hero would deal fire damage, it deals double that amount of damage instead." First damage-replacement aura (`hero_fire_damage_doubled`, doubling applied in `defer_packets` via per-packet `dmg_type`) — see the effects-table row.
- **Chromatic Cloak** (`azeroth_282`, 4, Armor — Cloth, Back (1), DEF 0) — "If your hero would deal damage with an ability, it deals that amount of damage plus 1 instead." Second replacement aura (`hero_ability_damage_bonus:1`, keyed on the `from_ability` packet tag; +1 before World in Flames' doubling) — see the effects-table row.
- **Mana Agate** (`azeroth_57`, 2, Ability — Arcane, Mage) — "Ongoing: 1, Destroy Mana Agate → Draw two cards" (`ongoing|activated_power:1:draw:2:::sacrifice_self`). First hero_row `sacrifice_self` power. AI fires it only when the hand has room for both cards (the draw gate in `_get_ally_power_actions` is amount-aware: hand ≤ max_hand − amount).
- **Arcane Intellect** (`azeroth_47`, 2, Instant Ability — Arcane, Mage) — "Attach to target hero, and its controller draws a card. Ongoing: Attached hero's controller's maximum hand size is increased by three" (`ongoing|attach:hero|attach_draw:1|attached_max_hand:3`). First hero-only attach and first max-hand-size modifier — see the `attach_draw` / `attached_max_hand` effects-table rows.
- **Aimed Shot** (`azeroth_32`, 1+X, Ability — Marksmanship Talent, Hunter) — "Your hero deals X ranged damage to target hero or ally" (`deal_damage_to_target:X:ranged`). First X-cost hand card — see the `deal_damage_to_target:X` effects-table row — and first Talent (deck legality: rule 100.2c in `DeckManager.authorize_deck_def`).
- **Spirit Bond** (`dark_portal_39`, 1, Ability — Beast Mastery Talent, Hunter) — "Ongoing: At the start of your turn, if you have a Pet, your hero heals 2 damage from itself and each of your Pets" (`ongoing|turn_start_heal_hero_and_pets:2`). Second Talent (the other spec, exercising the 100.2c mixing check).
- **Hannah the Unstoppable** (`azeroth_187`, 3/3 Ally) — "Opposing heroes and allies can't protect." Live static aura (`opposing_cant_protect`): while an opponent controls Hannah, that player has no legal protectors (`_protect_locked` short-circuits `get_legal_protectors`). Mirror of Jaina's aura, locking *protecting* instead of *attacking*.
- **Instant Ally** — Tristan Rapidstrike (azeroth_221, 3/3 Protector). The Instant tag (CSV `type` = "Instant Ally" → `CardDef.is_instant`) makes `play_ally` instant-speed in `_can_play_non_instant`: playable any time with priority (combat windows, opponent's turn, non-empty chain — rule 409.1). Resolves as a normal ally (summoning sickness, which does NOT block protecting — 601.2a restricts attackers only). AI: tagged `combat_instant_protector` in `BaseAI.COMBAT_INSTANT_TAGS` (held, never blind-played); `instant_protector_action` flashes it in during the ATTACK window only (never defend — the protect point is past) when being attacked, no board protector answers, and the protector decision tree would pick it (safe kill-and-survive block, or any block to save the hero); the normal `choose_protector` then uses it at the protect point.
- **Ally activated powers with sacrifice-style costs** — Kena Shadowbrand (`azeroth_190`, 1/3): `[Activate]`, put 1 damage on herself → draw (`activate_put_damage_self:1`; keeps the tap/exhaust, may be self-lethal per 405.3; AI won't draw her to death). Bizzik Sparkcog (`azeroth_178`, 2/4): `[Activate]`, destroy an ally in your party → draw (`sacrifice_ally` cost + `friendly_ally` target picker; AI only sacrifices an already-damaged ally). Augustus Corpsemonger (`azeroth_177`, 3/4): `[Activate]`, remove three ally cards in your graveyard from the game → destroy target ally (`destroy_ally` effect + `rfg_allies:3` cost; the 3 exiled cards are auto-chosen — see `data/rules_deviations.md`; AI destroys enemy allies worth killing via `_destroy_is_worth_it`). Gertha, The Old Crone (`dark_portal_164`, 1/3 Gnome Warlock): `1, [Activate]`, destroy an ally in your party → destroy target ally (`destroy_ally` effect + `sacrifice_ally` cost). First **two-pick** ally power — the sacrifice is a separate `sacrifice_id` param (Bizzik's single-target sacrifice keeps riding `target_id`), picked ahead of the destroy target in a two-phase targeting flow (`_is_ally_sacrifice_destroy_power` in `input_router.gd`, mirroring the damage_and_heal two-phase). Like `sacrifice_self`, the cost is paid at resolution — a sacrifice killed in response no-ops but the destroy still resolves. AI (`_get_ally_power_actions` destroy_ally + sacrifice_ally branch): sacrifice our lowest-value own ally (never Gertha herself), destroy the enemy's highest-value ally, only when the target outvalues the sacrifice; random tiebreak on equal value.
- **Rod of the Ogre Magi** (`azeroth_332`, 4, Two-Handed Weapon—Staff, Melee (1), 1 ATK) — first "power weapon": `[Activate]`, exhaust your hero → deal 1 damage to target hero or ally (`activated_power:...:exhaust_hero`, same path as Mooncloth Robe + Grimdron). Carries `two_handed` (first 414.3c Two-Handed↔Off-Hand uniqueness card) and the `power_weapon` AI flag (AI uses the ping, never strikes with it) — see both effects-table rows.
- **The Hammer of Grace** (`azeroth_323`, 3, Weapon—Mace, Melee (1), 1 ATK / 3 Strike, DrPaPrSh) — power weapon #3: `1, [Activate]`, exhaust your hero → your hero heals 2 damage from target hero or ally (`activated_power:1:heal_target:2::hero_or_ally:exhaust_hero`, same `exhaust_hero` path as Rod; first `heal_target` activated power). Carries the `power_weapon` AI flag (bad 1-ATK/3-strike; AI uses the heal, never strikes). AI heals its own damaged characters only (existing `heal_target` friendly-only branch in `_get_ally_power_actions`) — no new engine or AI code, purely a CSV recipe.
- **Hypnotic Blade** (`azeroth_327`, 2, Weapon—Dagger, Melee (1), 1 ATK) — power weapon #2: `3, [Activate]`, exhaust your hero → target player discards a card, use only on your turn (`activated_power:3:discard_opponent:1:::exhaust_hero` + standalone `on_your_turn` segment). The activated-power `discard_opponent` effect reuses Mias the Putrid's pending-discard machinery; "target player" is auto-chosen as the opponent — see `data/rules_deviations.md`. AI uses it whenever the opponent holds cards. In hotseat, the off-screen player's forced discard runs in **discard peek mode** (see Hotseat section).

### Playtested heroes (confirmed working)
Alliance : Boris, Dizdemona, Moonshadow
Horde : Ta'zo, Grennan, Boris Brightbeard, Omedus, Radak Doombringer

---

## Equipment (rules 300.1, 303/304, 414.3)

Equipment implemented: **Mooncloth Robe** (`azeroth_298`, Armor—Cloth, Chest, DEF 0), **Pads of the Dread Wolf** (`dark_portal_260`, Armor—Leather, Feet, DEF 1 — first card with functional DEF), **Golem Skull Helm** (`azeroth_290`, Armor—Plate, Head, DEF 3, vanilla blocker), **Draconian Deflector** (`azeroth_285`, Armor—Shield, Off-Hand, DEF 4, "Your hero has protector" — see `hero_has_protector` in the effects table), **Green Whelp Armor** (`azeroth_291`, Armor—Leather, Chest, DEF 1, first *triggered* equipment power — see `whelp_bounce` in the effects table), and **Devilsaur Leggings** (`azeroth_284`, Armor—Leather, Legs, DEF 1, "When your hero deals combat damage to an ally, destroy that ally" — mandatory conclusion trigger, see `hero_combat_dmg_destroys_ally` in the effects table). Weapons with a triggered power: **Iceblade Hacker** (`azeroth_328`, Weapon—Axe, Melee (1), 2 ATK / strike 2, "When your hero deals combat damage to an ally, that ally can't ready during its controller's next ready step" — mandatory conclusion trigger applying Gouge's ready-lock, see `hero_combat_dmg_locks_ally_ready` in the effects table).

- **Play flow:** `play_equipment` action (mirrors `play_ally`'s action-phase timing) → `_resolve_play_equipment` moves the card to the controller's `hero_row` (rule 304.1). Card type is `Equipment`; the recipe carries an `equipment:SLOT:DEF` segment.
- **Activated powers reuse the ally-power path** (`use_ally_power` / `_can_use_ally_power` / `_resolve_use_ally_power`), now generalized to accept a source in `ally_row` OR `hero_row`. Equipment has no summoning sickness. A power's optional 7th recipe field is an `EXTRACOST` token; `exhaust_hero` (Mooncloth Robe, Rod of the Ogre Magi, The Hammer of Grace) requires a ready hero at validation. **Exhaust-style costs are paid at ANNOUNCEMENT** (rule 412.2, `_pay_activate_costs` in `submit_action`, undone by `_refund_activate_costs` on retraction): the `[Activate]` tap symbol on the source, the `once_per_turn` mark, and `exhaust_hero`. Paying them at resolution instead left the source and the hero READY while the power sat on the chain, so the same power — or two `exhaust_hero` powers off one hero — could both be announced and both resolve off a single exhaust. The *sacrifice*-style costs (`put_damage_self`, `sacrifice_ally`, `sacrifice_self`, `rfg_allies`) deliberately stay at resolution, where a source killed in response no-ops the cost while the effect still resolves. New `draw` effect draws `AMOUNT` cards.
- **Slot uniqueness (414.3):** at most one equipment per slot. `_check_equipment_uniqueness` fires after an equipment enters play; a second same-slot equipment sets `pending_equip_sacrifice_player`/`_ids` and emits `equipment_sacrifice_required`. Resolve via `StackResolver.choose_equipment_sacrifice()` (called directly, not through the stack — same pattern as pet sacrifice). `can_submit` blocks everything else while pending. Note: with only one Chest card in the pool this can't yet trigger in a real game — it's covered by a headless test.
- **UI:** `input_router` `_action_type_for` maps Equipment → `play_equipment`; the context-menu "Activate Power" entry now covers hero_row equipment; equipment-sacrifice mode mirrors pet-sacrifice mode (red highlight, `start_equipment_sacrifice_mode`). `playtest.gd` handles `equipment_sacrifice_required` (AI keeps highest-cost) and the pass button shows "Destroy equipment". Hero_row rendering already spreads non-hero cards, so equipment displays with no renderer changes.
- **AI:** BaseAI now generates `play_equipment` (via `_action_type_for`) and equipment activated powers (`_get_ally_power_actions` scans hero_row too). The `draw` power is skipped when the hand is already full. This is simple-heuristic level (FullRandomAI picks among legal actions; GenericAI still prioritizes safe kills first) — the AI plays the robe and may draw with it, but doesn't reason about hero-exhaust tempo. Covered by `_test_ai_plays_equipment`.
- Tests: `_test_mooncloth_robe_power`, `_test_mooncloth_robe_hero_exhausted`, `_test_equipment_slot_uniqueness` in `test_scenarios.gd`.

### Armor damage prevention (rule 717.2c) — the prevention point

Prevention is a **decision point at the moment a damage packet would hit a hero** — never a chain action ("None of this uses the chain"), and never pre-committed. Direct-call pattern like the strike point:

- **The packet pipeline (`StackResolver.defer_packets`)**: EVERY effect that deals damage — instants, abilities, ally/hero powers, enter-play effects, totem triggers, end-of-turn burns, death-trigger AoE — hands its packets (`{source, target, amount, …hooks}`) to `defer_packets(state, db, packets, after, recursive_destroy)` instead of calling `GameLogic.deal_damage` (see the INVARIANT comment on `deal_damage`). If a packet would hit a hero whose controller has ready DEF>0 equipment (`get_ready_def_armor`), the point opens before the group lands; otherwise the group lands immediately (identical to dealing directly). Groups queue (`pending_prevention_deferred`); per-packet hooks run post-land off the damage actually DEALT: `riders` (Frostbolt restrictions), `discard_per` (Mind Spike/Blast, Ismantal), `drain_heal_per`/`drain_heal_to` (Steal Essence). After-hooks: `"totem_next"` opens the next queued totem trigger. **This makes new damage effects armor-preventable by construction — never call `deal_damage` from a resolution.**
- **Combat** keeps its own open site: when the defend window closes, BEFORE `_do_combat_conclusion` (`_combat_prevention_offers` mirrors the conclusion's damage math incl. Long-Range; both heroes can get sequential offers in a hero-vs-hero fight). Conclusion then runs from `choose_prevention` (resume `"combat"`).
- **Resolution:** `StackResolver.choose_prevention(state, armor_id, db)` — exhausts the armor, adds DEF to `PlayerState.damage_prevention`, and **re-opens the point** while damage remains and more ready armor exists ("you may exhaust another equipment, and so on"). `""` = take the remaining damage. `can_submit`/`pass_priority` hard-block while `pending_prevention_player` is set. Event: `prevention_opened` (re-emitted with the reduced amount per exhaust).
- **`GameLogic.prevent(state, db, source, target, amount, opts)` is the ONE place prevention is applied** (rule 717.2), called from `deal_damage` before any damage is placed. It returns `{"amount", "events"}` — what actually reaches the target plus the `damage_prevented` events — and applies, in order: unpreventability (short-circuit, absorbs nothing), character-side shields (Brother Rhone), then the armor pool. `opts` carries `unpreventable` (explicit override; otherwise DERIVED from the source, so new damage effects respect Lionheart Helm by construction) and `combat_attack`. **Unpreventable damage consumes no shield** — an unspent armor pool and a Rhone-style shield both survive it.
- **Consumption:** unchanged — `GameLogic.deal_damage` absorbs from the pool when the target is the controller's **hero** (allies are never protected), emitting `damage_prevented`. Excess DEF is wasted: the pool is cleared right after the packet lands. `discard_per_damage` / `drain_heal_per_damage` / whelp-bounce / `on_combat_damage_to_hero` all key off damage actually DEALT, so full prevention (packet ceases to exist, 717.2b) fires none of them.
- **AI** (`BaseAI.choose_prevention(state, db, player_id) -> String`, called by the scene/sim at the point): exhaust the highest-DEF ready armor for which `remaining >= DEF − 1` (avoids wasting big armor on chip damage); one armor per call, the re-opened point asks again (6 incoming vs DEF 3 + DEF 1 → both get used).
- **UI (playtest.gd):** `prevention_opened` → `_handle_prevention` — AI auto-resolves; a human gets the centered choice popup ("N damage incoming — exhaust armor?" with one button per ready armor + "Take the damage"), armors highlighted and clickable (`_in_prevention_mode`). Board-public, so the off-screen hotseat player is prompted inline too.
- Tests: `_test_pads_block_combat`, `_test_pads_block_decline`, `_test_pads_block_instant`, `_test_pads_block_enter_play_damage`, `_test_pads_overblock_expires`, `_test_ai_armor_block_heuristic`, `_test_prevention_noncombat_sources`, `_test_prevention_reduces_discard_per_damage`.

### Weapons & striking (rules 303, 602.1, 602.3) — hero combat

First cards: **Krol Blade** (`azeroth_331`, Weapon—Sword, Melee 1H, 3 ATK, strike cost 1, vanilla) and **Gorebelly** (`azeroth_9`, Horde Warrior hero, 30 HP, flip: pay 1 → next melee strike this turn costs 3 less).

- **Recipe:** `equipment:melee_weapon:0|strike_cost:STRIKE_COST`. Weapons play via the normal `play_equipment` path into the hero row; slot uniqueness reuses `_check_equipment_uniqueness`.
- **Strike points (don't use the chain):** striking is legal at exactly two moments — as the combat step starts, for the **attacking** player (602.1: in `_resolve_propose_combat`, after the attacker exhausts, BEFORE the attack window opens) and as the defender enters combat, for the **defending** player (602.3: in `_open_defend_window`, after the protect point, BEFORE the defend window). If the wielder is a hero whose controller has a ready, affordable weapon (`get_strikeable_weapons`), `pending_strike_player/_weapon_ids/_side` are set and `strike_point_opened` fires; `can_submit`/`pass_priority` hard-block while pending. The scene resolves via `StackResolver.choose_strike(state, weapon_id, db)` (direct call, like `choose_protector`; `""` = decline). The strike exhausts the weapon + pays strike cost (`weapon_struck` event), then the held window opens.
- **Strike modifier (303.2b):** `GameState.combat_struck_weapons` (wielder → weapon ids) feeds `get_atk` live (+weapon ATK, never cached); cleared in `_do_combat_conclusion` (303.2a). Only heroes are wielders; the hero may be exhausted; one weapon per combat (303.2c). A defending hero that strikes deals combat damage back (Long-Range attackers still zero it).
- **Legal attackers:** the `get_legal_attackers` 0-ATK hero gate now also admits a hero with an affordable strikeable weapon — so a hero is an available attacker only when the strike is actually payable (opponent resources are public, so AI forecasting works both ways).
- **Gorebelly flip** (`melee_strike_discount:3`): sets `PlayerState.melee_strike_discount`; `get_strike_cost` applies it to Melee weapons; consumed by the next melee strike, cleared at every turn start. AI only flips when net save > 0 and the discounted strike is payable (`_strike_discount_worth_it`) — never with Krol Blade (strike 1).
- **AI** (`BaseAI.choose_strike_weapon`, called by the scene/sim on `strike_point_opened`): attacking → always strike with the highest-ATK offered weapon; defending → strike when the counter-damage kills the attacker OR the attacker is the opponent's LAST legal attacker (nothing left to save it for), hold otherwise; never strike defensively vs Long-Range. `BaseAI.forecast_atk` (get_atk + best strikeable weapon) feeds attack proposals, `combat_trade_value`, and `find_safe_lethals` on both sides.
- **UI (playtest.gd):** strike point mirrors the protect point — inline buttons over the pass bar ("WeaponName (+ATK, cost)" / "Don't strike"), weapon highlighted and clickable; `_drain_passes`/`_schedule_next_turn` stall while `pending_strike_player`/`_in_strike_mode`.
- Tests: `_test_weapon_attack_strike`, `_test_weapon_defend_strike`, `_test_strike_gates_and_gorebelly_discount`, `_test_ai_strike_decisions`.

---

## Hotseat / Duel Table (playtest.gd)

Two humans on one screen: menu Player 2 type "Human (hotseat)" (both human ⇒ `_hotseat`).
`_local_player` is the seat the screen belongs to — hand visibility (`BoardRenderer.set_perspective`
+ `refresh_hand_visibility`) and input (`InputRouter.setup`) follow it; every formerly
p1-hardcoded UI/turbo/wrap-up check in playtest.gd now uses `_local_player` (so a single
human can also sit in the p2 seat vs an AI).

- **Rotating camera (TTS-style):** the board is world-space seen through `_camera`
  (Camera2D, `ignore_rotation = false`); ALL UI lives in the `_hud` CanvasLayer (layer 10)
  and never rotates. `_orient_camera(pid, animate)` turns the view 180° about
  `BOARD_PIVOT` (960,475) for the p2 seat (rotated camera pos = `2*PIVOT − (960,540)`).
  Called from `_begin_handoff` (animated, while the overlay is up), `_set_local_player`,
  and game setup (a single human seated at p2 vs AI gets the flipped view all game).
  `BoardRenderer.view_rotation_degrees` mirrors the camera so transient world overlays
  (targeting cursor, floating damage/heal numbers) counter-rotate and drift up-screen;
  the Alt+hover inspector lives in its own CanvasLayer (15). **Mouse → world must be
  camera-aware:** `CardNode` uses `get_global_mouse_position()`, `BoardRenderer` (extends
  Node, not CanvasItem) uses `_world_mouse()`; never use the raw viewport mouse position
  for world positions. New UI elements go in `_hud` (`_add_label(..., hud=true)`), new
  board elements in the scene root.
- **Handoff timing:** in hotseat the handoff fires at the START of the outgoing player's
  end phase (`phase_changed` → "end"), so the incoming player gets the wrap-up priority
  window (instants with leftover resources) plus their ready/draw on their own screen.
  `_stop_for_end_window` (set on confirm) keeps Turbo from auto-passing that window;
  cleared on their pass or the next phase change. If the outgoing player is over max
  hand size (wrap-up discard needs THEIR hand, rule 503.2a), the end-phase handoff is
  skipped and the `turn_changed` handoff below covers the switch instead. Ctrl+Enter
  is an alias for Ctrl+Space (wrap up / end turn).
- **Handoff:** on `turn_changed` to the other human, `_begin_handoff` hides BOTH hands
  (perspective sentinel `"__handoff__"` matches neither hand zone), sets `_handoff_pending`
  (blocks `_try_pass`, `_schedule_next_turn`, `_drain_passes`, `_maybe_turbo_pass`,
  `_do_ai_turn`, `_do_turbo_pass`, all key input; `CardNode.input_blocked` blocks clicks)
  and shows a fullscreen confirm overlay. Confirming runs `_set_local_player` then resumes.
- **Mulligan:** humans decide sequentially, each behind their own handoff
  (`_mulligan_queue` / `_advance_mulligan_queue`; `_mulligan_current` owns the panel).
- **Combat stance (per-player, set while seated; toggle next to Speed Mode):**
  `"passive"` — the off-screen player's priority windows auto-pass, driven by
  the same paths that drive AI (`_do_ai_turn` null-AI branch, `_drain_passes` hotseat
  branch). `"ambush"` (default) — a window STOPS when the off-screen player has a legal instant
  response (`_offscreen_has_play` probes the router with `local_player` swapped):
  `_enter_ambush_mode` points the InputRouter at them (rules make only instant-speed
  plays legal off-turn) with YELLOW highlights (`AMBUSH_HIGHLIGHT`), while the renderer
  perspective stays with the seat — their hand stays face-down, a playable card's front
  shows only while hovered (`_on_card_hover_scene`). The stop covers combat windows AND
  the CHAIN-response point right after a combat proposal (before the attack window) —
  where Litori's freeze / Exhaustion can still fizzle the proposal via the 601.3 recheck.
  Critical guard: the ambush stop re-points the router at the responder DURING the
  submission's own event emission, so `InputRouter._pass_own_proposal` also checks
  `action.source_player != local_player` — without it the proposer's auto-pass fires
  through the re-pointed router, spends the responder's priority, and resolves the
  proposal unanswered. The top-right **Skip button**
  (visible only during the stop) passes for them without revealing anything. Windows
  with no legal response still auto-skip. Mode exits when priority leaves the ambusher
  (`_refresh_ui` guard) or on Skip; `_drain_passes`/`_schedule_next_turn` freeze while
  `_in_ambush_mode`. `_stance` survives rematches.
- **Mandatory-choice routing (`_route_choice`) — the one funnel:** the engine
  already addresses every pending choice to a specific player
  (`pending_*_player`, and `pending_reveal_pick_chooser` where the decider differs
  from the owner), so the scene's only job is turning "who decides" into "how do I
  collect it here". `_route_choice(decider, visibility)` returns:
  `"ai"` (decider is an AI — the caller auto-resolves via the AI hook),
  `"local"` (the human in this seat — render inline), or
  `"peek"` (a human who is NOT in this seat — hotseat: `_enter_choice_peek_mode`
  re-points the InputRouter at them so their clicks resolve the choice).
  `visibility` is `"private"` (a mandatory discard — their hand stays face-down
  with one-card-at-a-time hover peek, gated on `_choice_peek_hides_hand`) or
  `"public"` (The Princess Trapped's reveal — both players are meant to see the
  cards, so nothing is hidden and no handoff happens). `_try_pass` is hard-blocked
  while peeking (the seated player's Space must not act through the re-pointed
  router); exits via `_exit_choice_peek_mode` (`_on_discard_mode_ended`, and the
  reveal browser's confirm) — router back to `_local_player`, hand visibility
  re-hidden. **A network build replaces the `"peek"` branch alone** (serialize the
  choice, await the remote player's answer); no card-level code changes. New
  mandatory choices should go through this funnel rather than adding another
  `if _hotseat and pid != _local_player` branch.
- **Hand hover magnify:** the local player's own hand cards scale ×1.2 (`HOVER_MAGNIFY`
  on top of `HAND_CARD_SCALE`) + z-bump while hovered; hover/unhover signals are wired
  in `_spawn_card_node`.
  Protect / strike / ready points are board-public and still stop for the choice inline.
  Known v1 limits: a totem trigger owned by the off-screen player is resolved on the
  shared screen (social contract).
- `_played_this_action_phase` is a per-player Dictionary (was `_p1_played_this_action_phase`).

---

---

## Graveyard browser (playtest.gd `_open_gy_dialog`)

The one modal used by every graveyard/deck search — quest rewards, hero powers
(Sen'zir), ally powers (Ophelia), hand abilities (Ancestral Spirit,
Cannibalize), the reveal-and-pick quests, and the read-only examine/Alt-peek.

- **Sections.** The body is a VBox of sections (`_gy_body`), each an optional
  heading plus its own grid. A search whose requirement is `owner:both` is
  split into **Your graveyard** / **Opponent's graveyard** — a flat grid gave
  the player no way to tell whose card was whose, which is the whole decision
  for Cannibalize and Ophelia. An empty side still renders its heading with
  "— empty —" so "nothing over there" is visible rather than inferred. Every
  other search passes no sections and renders exactly as before.
- **Confirmation step.** `owner:both` searches also set `_gy_ask_confirm`:
  Confirm raises a centered "Remove 3 ally cards from the game and heal 6
  damage?" popup (`_show_gy_confirm_popup`, z 25 — above the browser's
  dimmer/panel at 19/20) before anything is submitted. The heal clause appears
  only when the card carries `rfg_heal_per_card`. **Cancel is free**: nothing
  has reached the engine, so the browser is still open with the selection
  intact, and Esc / right-click-outside dismisses the popup layer first
  (`_on_gy_cancel_pressed`) rather than abandoning the search.
- **Multi-select submission.** `InputRouter.confirm_graveyard_selection`
  announces `target_ids` for exile abilities (`_ability_graveyard_dest` ==
  `"rfg"`) and `target_id` for the single-pick reanimate/power/quest paths.

## Card muting (auto-pass convenience — InputRouter.muted_ids)

Any of the local player's cards can be **muted** via the right-click context menu
("Mute (don't hold priority windows)" / "Unmute"). Muting is pure UI state
(`InputRouter.muted_ids`, instance_id-keyed, cleared on a new game, shared by both
hotseat seats) — the card stays fully playable and keeps its green highlight, but
`has_any_legal_play(true)` (the `exclude_muted` variant used by Turbo layer 3 in
`_drain_passes` and the hotseat ambush stop `_offscreen_has_play`) ignores it, so a
muted card alone never holds a priority window open. Use case: an always-completable
cost-only quest or a power the player never intends to fire at instant speed otherwise
forces a "Skip" click at every window. The player can still play a muted card during
standard windows, or in any priority window another (unmuted) card keeps open.
`toggle_mute` is a context-menu pseudo-action handled in `handle_context_action`
(never submitted to the engine); `card_mute_changed` drives a 🔇 badge on the
CardNode (`set_muted`). The pass-button label (`_update_pass_btn`) still counts muted
cards as legal plays. Muting follows the instance across zones (mute a quest in hand,
it stays muted in the resource row).

---

## Combat window rules (enforced in multiple layers)

Rule 601.1 (combat only during non-combat action phase) and rule 502.1/1199 (non-instants same) are enforced in three layers:

1. **`stack_resolver.gd` — submission guard** (`_can_propose_combat`, `_can_play_non_instant`, `_can_play_ability`, `_can_place_resource`): all check `not (combat_attack_window or combat_defend_window or in_protect_point)` and `pending_actions.is_empty()`.
2. **`input_router.gd` — highlight guard** (`get_playable_card_ids`, `can_attack` in context menu): same flags, so allies don't stay green during combat windows.
3. **`input_router.gd` — direct click guard** (`handle_card_click`, in-play ally/hero branch): `can_propose` check before calling `get_legal_attackers`, prevents entering targeting animation when `pending_actions` non-empty or combat window is open.

**Attack animation** (`_animate_attack` in `board_renderer.gd`) fires on `combat_concluded` (not `combat_started`) so the lunge plays toward the actual defender after any protector swap, not the proposed defender before the protect point.

**Cancelled combat (rule 603.1b):** when a combatant leaves play before the conclusion (Withdraw bounce, instant removal, Blink's remove-from-combat), `_do_combat_conclusion` emits `combat_cancelled` (payload `attacker_id`/`defender_id`/`reason` ∈ `attacker_gone`/`defender_gone`/`attacker_removed`) followed by `combat_concluded` with `cancelled = true` and 0/0 damage. The renderer **skips the attack lunge** on a cancelled conclusion, and `playtest.gd` shows a centered transient notice (`_show_combat_cancelled_notice` → `_show_transient_notice`, a non-blocking self-fading HUD banner) plus a game-log line. Any new "combat fizzles" path must go through this branch so the UI stays honest.

**Window-opened events must drain (playtest.gd):** both `attack_window_opened` and `defend_window_opened` handlers call `_drain_passes()`. When the *human's* pass is the one that resolves the pending action, `pass_priority` returns only the resolution events (no `priority_passed`), so nothing else re-drives the AI — priority sits on the AI forever and the game stalls. `_drain_passes()` is re-entrancy-guarded (`_draining`), so calling it from a handler fired mid-drain is safe. Any new event that hands priority to a potentially-AI player needs the same treatment.

---

## Display / layout

- Viewport: **1920×1080**, `stretch/mode=canvas_items`, `stretch/aspect=expand` (fills 16:9 window, no grey bars).
- All card spawn positions and deck back sprites are derived from `_renderer.zone_anchors` — **never hardcode a spawn Vector2**. `_spawn_zone_nodes(zone_id, color)` reads the anchor automatically. The deck back sprite loop does the same via `_renderer.zone_anchors.get("p1_deck"/"p2_deck")`.
- At startup, `_spawn_zone_nodes` is called for every visible zone: hero_row, hand, ally_row, resource_row, graveyard, chain (deck excluded — back sprite only).
- UI bar occupies y=960..1080. Board occupies y=0..950. `CENTER_X = 960`.
- **Symmetric board (all modes, incl. vs AI):** each player's hero/deck/graveyard column
  sits on THEIR right-hand side — P1 at screen-right (deck x=1820, grave x=1640,
  hero_card (1820,620)), P2 at screen-left (deck x=100, grave x=280, hero_card (100,280)).
  The game log is a HUD overlay on the left gutter (covers P2's column), hidden by
  default — L toggles it.
- **Card facing (all modes):** cards in `p2_*` zones render rotated 180° (facing P2,
  TTS-style). `CardNode.facing_degrees` is the base every rotation write composes with:
  exhaust = facing+90, ready/settle = facing. Facing is set from the zone prefix in
  `place_card_in_zone`, `_animate_move` (side change = control change), and
  `reconcile_from_state` (`BoardRenderer._facing_for_zone`). Never write a card's
  `rotation_degrees` with a bare 0/90 — always compose with `facing_degrees`.
  The hero HP bar follows the same facing: P2's bar is rotated 180° about its own
  center (`_ensure_hero_bar`), so its label/fill read upright for P2.
- Pass button shows **"Sacrifice a pet [Space]"** (disabled, no color change) when `pending_pet_sacrifice_player == "p1"`.

---

## Win conditions (rules 102.1a, 410.6b)

A player loses immediately when:
- **`hero_defeated`** — their hero takes fatal damage / is destroyed. Emitted at
  the damage and destroy sites in `stack_resolver.gd`.
- **`decked`** — they are *required to draw* from an empty deck (410.6b).
  **Emptying the deck is not itself a loss** — only the next required draw is.

`GameLogic.draw_one(state, player_id)` is the single "required draw" primitive;
every draw site (`StackResolver._draw_one`, `TurnManager._draw_one`,
`shuffle_hand_into_deck_and_draw`) delegates to it, so a new draw effect gets the
decked rule for free. It emits `deck_empty` + `player_decked` and calls
`GameLogic.mark_decked`, which records the player in `GameState.decked_players`
and ends the game. Effects that merely LOOK at the deck (`reveal_pick`) are not
draws and must not call it.

If every remaining player is decked at that moment the game is a **draw**
(`GameEvent.game_drawn`, payload `draw = true`, `winner = ""`).

**The `game_over` payload carries the win condition**: `winner`, `loser`,
`losers`, `reason`, `draw`. `GameEvent.game_over_explanation(payload, names)` is
the ONE place that turns it into a sentence ("P2's hero received fatal damage.
P1 wins!" / "P2 was decked — required to draw from an empty deck. P1 wins!"),
shared by the game-over dialog, the board status line and the game log. A new
win condition adds a `reason` branch there and nowhere else.

Tests: `_test_decked_loses_the_game`, `_test_empty_deck_alone_is_not_a_loss`,
`_test_simultaneous_decking_is_a_draw`, `_test_game_over_explanations`.
`_drive_turns` stocks both decks with filler so multi-turn scenarios don't deck
out (pass `stock_decks = false` for hand/graveyard-counting scenarios).

---

## Known open issues / next work

- **Combat window AI:** the non-turn AI now plays tagged **combat instants** (Quick Strike) during attack/defend windows via `BaseAI.combat_instant_action` — see AI conventions above. It still never uses ally activated powers (e.g. Grimdron) to counter-attack during enemy combats. Future: extend the window logic to defensive ally-power plays.
- **Next cards to implement:** Check `data/cards.csv` for candidates.
- **`logic/BasicAI_behavior.txt` is legacy** and may be stale — actual AI is fully in `game_logic/ai/base_ai.gd`.
